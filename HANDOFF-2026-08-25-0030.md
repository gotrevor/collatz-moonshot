# HANDOFF 2026-08-25-0030  (★ Parity-reconstruction project COMPLETE + classified ★)

> **Post-run audit correction (2026-08-24):** the Lean theorems and exact experiment are
> sound, but the original text overclaimed a universal finite-state no-go. The observed
> same-suffix spread proves only that `q/3^a` is not determined or uniformly approximated by
> a bounded suffix. A finite-state potential could still work through aggregate transition
> inequalities. Classification is therefore **BASELINE / RE-SCOPE**, not a proved REDIRECT
> theorem. `DIRECTION.md` now points to `FRONT-A-PARADOXICAL.md`.

- **Branch:** `main`  **Tree:** clean  **Build:** 🟢 `lake build` 8749 jobs.
- **Read first, in order:** `DIRECTION.md` (CURRENT DIRECTIVE — outranks this file),
  `PENDING_WORK.md` (top block = this project's classification), `FRONT-A-PARITY-RECONSTRUCTION.md`
  (RESULTS section appended at bottom), then this file.

## What these laps did: executed the mandated parity-reconstruction project end-to-end

DIRECTION.md's CURRENT DIRECTIVE mandated the experiment-first probe of
`FRONT-A-PARITY-RECONSTRUCTION.md` (a research probe, NOT a directive to claim W1′).  All of
it is now delivered and honestly classified.  `box done --green` signalled.

### New files
- `experiments/parity_reconstruction.py` — exact-integer reconstruction machine. Exhaustively
  cross-checks (depth ≤14) the modular residue `R(v)`, the online `(r,q,p,c)` carry
  recurrence, `traceWord`, the endpoint identity, prefix nesting, and the `R↔trace` bijection
  over all `2^m` residues. Deeper probes (depth ≤16): gap/positivity/no-go.
- `CollatzMoonshot/FrontA/ParityReconstruction.lean` — sorry-free, every theorem trust-base
  clean `[propext, Classical.choice, Quot.sound]`. Wired into `CollatzMoonshot.lean`.

### Proved in Lean (all trust-base clean)
- `tstep_iterate_lt` — **the carry invariant**: `tstep^[m] n < 3^(ones(traceWord n m))·(n/2^m+1)`
  for ALL n (strengthened form; the odd step is exactly absorbed by the floor factor where the
  naive first-bit induction breaks).
- `tstep_iterate_lt_pow_ones` — for `n<2^m`, `tstep^[m] n < 3^(ones)` (normalized endpoint
  `q/3^a < 1`, tight at all-ones `q = 3^a−1`).
- `normalized_endpoint_ne_start_one` — for every positive depth the fixed start `1` gives
  a kernel-checked counterexample to "the normalized endpoint stabilizes to the start".
- `two_pow_mul_gap` — exact carry-gap identity `2^m(3^a−q) = 3^a(2^m−n) − numer`.
- `pow_ones_mul_le` — matching drift lower bound `3^a·n ≤ 2^m·tstep^[m] n`.
- `traceWord_eq_imp_modEq`, `eq_of_forall_traceWord_eq` — residue determinacy (a natural is
  determined by its parity itinerary).
- `tstep_repeat_of_eventually_periodic_parity`, `tstep_periodic_of_..._parity`,
  and `not_diverges_of_eventually_periodic_parity` — the eventually-periodic no-divergence
  baseline, bridged (`exists_step_count`, `tstep_iterate_pos`) into a genuine `¬ Diverges n`
  statement in the repo's own vocabulary (project item 5, complete).

### Strength-audit findings (exhaustive experiment)
- `q < 3^a` is the SHARPEST uniform bound — exact gap `3^a−q = 1` at every depth.
- **Bounded-suffix endpoint collision:** two words sharing their last 3 bits realize
  `q/3^a` spanning nearly `[0,1)` (spread ≈0.9997 at depth 16). This refutes suffix-only
  endpoint prediction; it does not refute every finite-state Lyapunov architecture.
- The tested positivity proxy is weak: words with top output bit zero still reach odd
  density `(m−1)/m → 1 > log2/log3 ≈ 0.6309`.

### Classification (post-audit): BASELINE / RE-SCOPE
The exact reconstruction API is complete in Python, while Lean formalizes the useful forward
kernel and orbit consequences. The invariant is the known critical envelope, not a divergence
obstruction. No stronger certificate was found. The experiment warns that suffix-only endpoint
models discard essential information, but does not prove all bounded-state certificates fail.

## Next steps (for an altitude re-scope — NOT for a plain lap)

1. **The live Front-A crux is still `ParityRigidityW1'`.** The next funded lens is
   `FRONT-A-PARADOXICAL.md`: use exact finite segments in which the subcritical coefficient
   is overcome by the additive remainder, reproduce the 2026 results, and seek new structural
   restrictions. Do not confuse the cited wiring or a finite census with a Collatz advance.
2. A concrete, non-forbidden sub-probe if a future directive wants pathwise→measure glue:
   the carry invariant gives, by logs, `Diverges ⇒ liminf (odd density) ≥ 1/log2 3` — but this
   is the EASY necessary direction and does not advance W1′; noted, not pursued.
3. **Front B `Compression`** — still blocked on the Simons–de Weger 2005 source
   (`ON-LINE-REQUEST.md` unanswered). Apparatus feature-complete; do not touch until the source
   lands or a new upper-bound idea appears.
4. **Do NOT** re-run harmonic-dual (COMPLETE), rebuild M2′ (COMPLETE), or grind `OneCircuit`
   a≥2 (Steiner/Baker, off path).

## Axiom footprints (verified this session)
All new theorems in `ParityReconstruction.lean`: `[propext, Classical.choice, Quot.sound]`.
Repo headline axioms unchanged (`conjecture_of_fronts` trust base; `frontB_of_compression_le_91`
+ `hercher_min_circuit_count` 🟡; harmonic dual COMPLETE).
