# Handoff: PowApprox node proved — Front-A paradoxical closer trust-base clean; directive complete

**Date**: 2026-09-02 00:00 UTC · **Branch**: `main` · **HEAD**: `55a119b` (clean tree, nothing pushed — host pushes)

## 🎯 What we're doing
Formalizing the Collatz conjecture graph in Lean 4 / mathlib (`~/src/collatz-moonshot`).  The
binding objective in `DIRECTION.md` (set by the 2026-09-01 review lap) was to discharge the cited
axiom `Assumed.rozier_terracol_3_2` (Rozier–Terracol 2026, Thm 3.2) so that the Front-A closer
`finite_acyclicParadoxical_imp_noDivergent : FiniteAcyclicParadoxical → NoDivergentOrbit` is
trust-base clean.  **That objective is now complete.**  The treadmill has been told to stop
(`box done`, honored: source is sorry-free).  The next lap is an *altitude* lap: retarget
`DIRECTION.md`.

## 🧠 Context to carry forward
- **The kickoff instruction for the 2026-09-01 lap was stale.**  It described the
  `sep_two_three` ↔ `rhinLiteLIMeasure` re-wiring fork with operator points (1)–(3); that fork was
  already fully resolved at `d21f006` (`HANDOFF-2026-09-01-rhin-axiom-retired.md`): direct
  re-wiring refuted (existential `N₀`, opaque `c`), explicit-constant version built (`N₀ = 0`,
  `κ = 436`, `K = 141000`, five convergent brackets), κ-sharpening refuted low-leverage.  Lesson:
  always reconcile a kickoff against `git log` + newest `HANDOFF-*` + `DIRECTION.md` before acting.
- **The node was closed by a multiplicative pigeonhole, not the Dirichlet plan on file.**
  `two_pow_approx_three_pow_from_above : ∀ M N, ∃ A s, M < A ∧ 3^A < 2^s ∧ (2^s − 3^A)·N ≤ 3^A`.
  Ratios `2^(⌊log₂3^A⌋+1)/3^A ∈ (1,2]` at `A = i(M+1)`, `i ≤ N`, land in `N` boxes
  `((1+1/N)^j, (1+1/N)^(j+1)]`; a collision gives `a > M`, `b` with `2^b/3^a` and `3^a/2^b` both
  `< 1+1/N`; parity gives `3^a ≠ 2^b`; if the ratio is below 1, the least `u` with
  `(3^a/2^b)^(u+1) ≥ 2` flips it: `2^(ub+1)/3^(ua) ∈ (1, 3^a/2^b]`.  No `Real.log`, no
  irrationality of `log₂3`, no `native_decide`.  Pattern worth reusing anywhere a "good
  approximations exist" (density-type) statement is needed.
- **Assumption operating under:** `DIRECTION.md` is altitude-only; I did not edit it even though
  its objective is done.  `STATUS.md`/`PENDING_WORK.md`/README carry the completion notice.
- **What is NOT done and why it was not attempted:** the 13 `native_decide` certificates under
  `sep_two_three` / `le_two_blocks_not_acyclicParadoxical` (DIRECTION labels converting them to
  `decide +kernel` as hygiene, not a lap goal); the cited Front-B / computation axioms (Front B is
  marked blocked and mis-scoped in DIRECTION).  Neither is on the Front-A paradoxical path.

## ✅ State (all observed this session)
- `lake build` → `Build completed successfully (8765 jobs)` (also re-run by the pre-commit hook).
- `scripts/check-proof-debt.sh` → `0 disclosed sorries`.
- `lake env lean scripts/AxiomAudit.lean | grep -c sorryAx` → `0`.
- `#print axioms` = `[propext, Classical.choice, Quot.sound]` for
  `finite_acyclicParadoxical_imp_noDivergent`, `Assumed.rozier_terracol_3_2`,
  `diverges_imp_infinite_acyclicParadoxical`, `FrontA.two_pow_approx_three_pow_from_above`,
  and the three new lemmas (`exists_mul_box`, `exists_two_pow_three_pow_ratio_close`,
  `approx_from_above_of_ratio_close`).
- Commit `55a119b` on `main`: proof + doc de-staling (`Assumed/Paradoxical.lean`, README, STATUS,
  PENDING_WORK, both scripts) + `HANDOFF-2026-09-01-powapprox-node-proved.md`.
- `box done` → "Source is sorry-free — the host WILL honor this stop (no relaunch)."
- No Aristotle job in flight.

## 🎬 Next actions
1. **Altitude lap first**: rewrite `DIRECTION.md` → CURRENT DIRECTIVE.  The 2026-09-01 objective
   is complete; record it in "Directive history".  Candidates to rank, in the order I would rank
   them: (a) hygiene that makes the *whole* Front-A two-block exclusion bare trust base — convert
   the 13 `native_decide` certificates to `decide +kernel` where operand sizes allow (the
   `k < 450` table `sep_two_three_small_450` at `m < 713` is the likely hard one; the Rhin-lite
   `rhinLite*_certificate` lemmas may be small); (b) the actual mathematical frontier
   `FiniteAcyclicParadoxical` — read RT 2026 §4–5 for what they conjecture/prove about acyclic
   paradoxical segments and see whether the repo's "≥ 3 odd blocks" sharp result can be extended
   to a *density* or *height* bound; (c) Front-B citations (`hercher_*`, `eliahou_*`) — only if the
   host provides sources.
2. Do not restart proof work on the paradoxical node; it is closed and audited.
3. If the treadmill relaunches anyway, run `bash scripts/check-proof-debt.sh` and
   `lake env lean scripts/AxiomAudit.lean | grep sorryAx` first to confirm the clean state.

## ⚠️ Gotchas (this toolchain: Lean v4.33.1, mathlib as pinned in `lake-manifest.json`)
- `mul_lt_mul_left` is now the ordered-*monoid* lemma (`MulRightStrictMono`, which ℝ lacks).  In a
  field use `lt_of_mul_lt_mul_left h hc.le`, `mul_lt_mul_of_pos_left`, or
  `mul_lt_mul_iff_of_pos_left` (alias of `mul_lt_mul_iff_right₀`).  `gcongr` on a `calc` step
  `c * x < c * y` can fail with the same instance error — give the lemma explicitly.
- `field_simp` now closes `x * N = y * (N + 1)`-style goals outright; a trailing `ring` errors
  "no goals".  `simpa using h` rewrites `1 / N` to `N⁻¹` and then no longer matches a statement
  written with `1 / N`.
- `push_neg` deprecated → `push Not`, or just `not_le.1` / `not_lt.1`.
- `one_add_mul_le_pow (H : -2 ≤ a) n` — `H` is not a `positivity` goal; get `0 ≤ a` and `linarith`.
- Never `cd` inside a Bash call before `lake env lean` (toolchain resolves from cwd).

## 📁 Key files
- `CollatzMoonshot/FrontA/PowApprox.lean` — the proved node + three lemmas; module docstring is the
  proof sketch.
- `CollatzMoonshot/Assumed/Paradoxical.lean` — RT Thm 3.2 as a theorem; the Front-A wiring.
- `DIRECTION.md` — CURRENT DIRECTIVE (complete; needs retargeting by an altitude lap).
- `PENDING_WORK.md` — top section = this lap's record + "what is left" inventory.
- `STATUS.md` — axiom ledger table (re-run from real `#print axioms` on 2026-09-01).
- `scripts/check-proof-debt.sh`, `scripts/AxiomAudit.lean` — the two gates.
- `HANDOFF-2026-09-01-powapprox-node-proved.md`, `HANDOFF-2026-09-01-rt-axiom-fidelity.md`,
  `HANDOFF-2026-09-01-rhin-axiom-retired.md` — the three preceding laps.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait
for instructions, and don't offer other projects from the KB — this doc IS the chosen thread.
Absorb the context above, then pick up at "Next actions" #1: retarget `DIRECTION.md` as an
altitude lap, ranking (a)/(b)/(c) above, and start working.**
