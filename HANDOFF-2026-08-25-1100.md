# HANDOFF 2026-08-25-1100  (★ two-block crux reduced to the single inequality `b + d ≤ 5` ★)

- **Branch:** `main`  **Build:** 🟢 `lake build` 8751 jobs.  **Tree:** clean.
- **Read first, in order:** `DIRECTION.md` (CURRENT DIRECTIVE — outranks this; do NOT edit),
  `PENDING_WORK.md` (top block = live state + refuted attacks + next moves), then this file.

## What this run did (grind on the mandated paradoxical crux)

Executed `DIRECTION.md`'s mandated move: the interior two-block exclusion
`le_two_blocks_not_acyclicParadoxical`.  Drove its residual from an opaque residue `sorry` all
the way down to **one clean inequality `b + d ≤ 5`**, proving everything else sorry-free.

### Landed sorry-free this run (`CollatzMoonshot/FrontA/Paradoxical.lean`)
1. **`near_critical_containment`** — the sharpened self-contained crux.  From the ¬gap witness
   `w` (`L ≤ 3^b·w`, `D·w ≤ U₁`) it proves, sorry-free:
   - the **elementary squeeze** `(2^m − 3^(b+d))·(2^d − 1) < 3^(b+d)` (×D, ×3^b, cancel `2^c`);
   - the **clean power bracket** `2^m·(2^d − 1) < 3^(b+d)·2^d`;
   - the **tight two-sided bracket** `3^(b+d) < 2^m < 2·3^(b+d)` (`hupper`);
   - then, **given `b + d ≤ 5`**, the whole containment: `hupper` ⇒ `m ≤ 8` ⇒ finite box, and
     `interval_cases b<;>d<;>c<;>e <;> norm_num <;> omega` (~800 cases, `maxHeartbeats 4000000`)
     forces `(b,c,d,e) ∈ {(2,3,3,0),(3,3,2,0)}`.
2. **`window_unique_m`** — the near-critical window holds **at most one** power of 2, so `m` is a
   function of `(b,d)` (dimension reduction any attack reuses).
3. The residual branch of `two_block_residue_core` now routes cleanly through
   `near_critical_containment` + `residue_core_exc1/2` (no opaque sorry).

### The SOLE remaining `src/` sorry (the live crux) — `Paradoxical.lean:515`
    have hbd : b + d ≤ 5 := by sorry
Everything else in the two-block exclusion is machine-checked.  This inequality is the pure
`2^m` vs `3^k` separation content — **effective irrationality of `log₂ 3`** (Baker).

### Refuted this run (do NOT retry — see `PENDING_WORK.md` for data)
- `D > U₁` for `b+d ≥ 6`: NO (84914 tuples have `D ≤ U₁`).  ⇒ a `|2^m−3^k|` separation lower
  bound on `D` **cannot** force `b+d ≤ 5` (route (b) with a simple gap axiom is INSUFFICIENT).
- `2D > U₁`: NO (397604 counterexamples; ceiling grows to ~`10^35`).
- real bound `D·L > 3^b·U₁`: NO (infinite strip, `d` grows to 9 at `b+d = 306`).
- Positive handle: for `b+d ≥ 6, D ≤ U₁` the ceiling `⌈L/3^b⌉` is always `≥ 4` (never `≤ 3`).

## Axiom audit (this run, `#print axioms`)
- `le_two_blocks_not_acyclicParadoxical`, `two_block_residue_core`, `near_critical_containment`:
  `[propext, sorryAx, Classical.choice, Quot.sound]` — `sorryAx` solely from the `b+d ≤ 5` sorry.
- **Headline `finite_acyclicParadoxical_imp_noDivergent`: `[propext, Classical.choice, Quot.sound,
  rozier_terracol_3_2]` — NO `sorryAx`.**  The two-block work is standalone discovery, correctly
  NOT wired into any headline.  The reduction chain (le_two_blocks → two_block_residue_core →
  near_critical_containment → `b+d≤5`) was read end-to-end and is faithful.

## UPDATE (2026-08-25-1200): independent corroboration + Baker diagnosis is now DEFINITIVE
- **Aristotle** (job `4006e40e…`, done) was handed `two_block_residue_core` and **independently
  reproduced the entire reduction** — same criteria A/B, same Case A, same single residual — and
  hit the identical wall: an effective linear-forms-in-logs (Baker) bound absent from mathlib. Its
  exhaustive evidence (1.35M genuine solutions; near-equality band `3^g<2^m≤2·3^g` to `g ≤ 119`)
  finds no counterexample. This is strong faithfulness evidence for our reduction AND confirms the
  remaining input is genuinely Baker-grade. Do NOT resubmit.
- **Faithfulness note acted on:** removed the unnecessary hypothesis `2 < n` from
  `two_block_residue_core` (Aristotle flagged it unused; build stays green 8751).
- **3-adic reframing** (`L mod 3^b` via `orderOf 2 mod 3^b`) is recorded in `PENDING_WORK.md` as a
  distinct angle, BUT the failure set has unbounded `b,d` and still carries the near-critical
  (Baker) constraint, so it does not escape the wall — it relocates it. Both views agree.
- **Bottom line:** `b+d ≤ 5` is a genuine multi-year Baker wall (independently validated), not an
  oversight. It is narrowed maximally: the two-block exclusion is proved modulo this ONE inequality,
  everything else machine-checked, headline `sorryAx`-free. This is the honest disclosed state — do
  NOT relabel it as a cited axiom just to clear the src-sorry gate (that games the anti-quit gate
  and, per DIRECTION, a cited implication is BASELINE, not the GO upgrade).

## Next moves (priority)
1. **Route (a) — GO upgrade:** prove `b + d ≤ 5` by building an explicit effective irrationality
   bound / continued-fraction certificate for `log₂ 3` in Lean.  Mathlib LACKS this (only the
   `LiouvilleWith` framework, no specific measure) — genuinely multi-lap.  It is a 2-dimensional
   Diophantine fact (`⌈L/3^b⌉·D > U₁`), not a 1-inequality separation; the CF convergents of
   `log₂ 3` with `2^m > 3^k` are at `k = 5,17,29,41,…` and only `k = 5` is loose enough.
2. An **Aristotle** attempt has been working the full
   self-contained core async since ~09:00.  If it returns a proof: kernel-verify with
   `#print axioms` (must be trust-base clean, no `sorryAx`/new axiom) BEFORE trusting, then inline
   to supersede the `b+d ≤ 5` sorry.  Do NOT resubmit.

## Other fronts — UNCHANGED (do not touch without a new directive)
- Front A M2′ COMPLETE; parity-reconstruction COMPLETE/BASELINE; harmonic-dual COMPLETE.
- Front B `Compression` on hold (SdW source-blocked).  `OneCircuit` a≥2 = Steiner/Baker, off path.
