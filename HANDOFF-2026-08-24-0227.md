# HANDOFF 2026-08-24-0227 — exponent-4/5 pipeline COMPLETE

- **Branch:** `main`  **HEAD:** `9808861`  **Tree:** clean
- **Build:** full `lake build` green, **8745 jobs**, no `sorry`, no project axioms.

## What's done this lap

Delivered the entire depth-8 exponent-`4/5` certificate → renewal → stopping
pipeline as one project (operator's scoped target:
`sorry-free:CollatzMoonshot/FrontA/BackwardFourFifthsStopping.lean`, met).

Three new modules, all imported from `CollatzMoonshot.lean`:

1. `CollatzMoonshot/FrontA/BackwardFourFifths.lean` — frozen 21,870-entry
   potential (5 floors `{1,7/4,3,6,12}` × 4,374 unit residues mod 6561, documented
   order); `fourFifthsChildResidue`/`oddBlockChild_mod_6561`/
   `oddBlockChild_sharedLift_6561` (child mod 6561 from source mod 19683 + shared
   digit `x/6561 % 3`); height geometry **reused verbatim** from the `3/4` module;
   all 65,610 inequalities via `native_decide` at scale `31250·1000000^23`
   (`fourFifthsNatCertificate`); rational bridge (`fourFifthsNatImage_eq`,
   `qpow_scale_4f`); `exists_fourFifthsExpansion`.
   NOTE: array needs `set_option maxRecDepth 400000`.
2. `CollatzMoonshot/FrontA/BackwardFourFifthsRenewal.lean` — `fourFifthsEdgeWeight
   = ((x)/(y))^(4/5)`; fifth-power comparison `fourFifthsRationalWeight_fifth_lt`
   (`w^5 < (3/2^j)^4` from `(75257/31250)^5 < 81`, `16·(574349/1000000)^5 < 1`);
   telescoping; potential positivity, uniform max 41080952;
   `exists_fourFifthsChildFinset`; `FourFifthsStoppingFrontier` /
   `FourFifthsRepeatOrStoppingGrowth` / `.card_bound`.
3. `CollatzMoonshot/FrontA/BackwardFourFifthsStopping.lean` — fuel-`H+1` frontier
   recursion, collision-to-cycle splice, no-low termination, exact mass growth,
   `fourFifthsRepeatOrStoppingGrowth`, `fourFifthsStoppingFrontier_of_noCycle`,
   potential minimum 1000000, and the pinned `fourFifths_stopping_card_bound`
   (constants 1000000/41080952) **unchanged**. State-independent value machinery
   reused verbatim from `BackwardStopping.lean`.

## Axiom audit (real `#print axioms`, 2026-08-24)

- `exists_fourFifthsExpansion` → `[propext, Classical.choice, Quot.sound,
  fourFifthsNatCertificate._native…]`
- `exists_fourFifthsChildFinset`, `fourFifthsRepeatOrStoppingGrowth`,
  `fourFifthsStoppingFrontier_of_noCycle` → + `fourFifthsPotential_pos_of_unit._native…`
- `fourFifths_stopping_card_bound` → + `fourFifthsPotential_ge_min_fin._native…`,
  `fourFifthsPotential_le_max_fin._native…`

No project axioms; identical footprint shape to the `3/4` pipeline.

## Commits this lap

- `7c13751` local certificate + `exists_fourFifthsExpansion`
- `f848cb6` renewal layer
- `01a4a1b` stopping pipeline (final corollary discharged)
- `9808861` HANDOFF/FRONT-A-ROUTES docs

## Exact next steps (next lap)

The ladder now stands `2/3 → 3/4 → 4/5`; `4/5 < 1`, so this is a rung, not the
headline. Open crux is unchanged: **can any fixed finite floor set reach harmonic
exponent `1`?** — the 3-adic adversary (`experiments/barrier_adversary.py`) drove
the measured slope *down* with depth, casting doubt.

1. Local probe: depth-9+ shared-lift search in `experiments/barrier_transfer.py`
   for a critical exponent strictly above `4/5` on these (or richer) floors. If
   found, the certificate→renewal→stopping port is now doubly mechanical (two
   rungs done identically — sed-transform the `4/5` trio, fix the power-comparison
   exponent and the frozen array/constants).
2. The real mathematics is steps 5–6 of `FRONT-A-ROUTES.md`: uniform vs. pointwise
   harmonic growth (the 3-adic adversary is the falsification side) and the
   overlap/packing question. Do NOT keep climbing rungs if the adversary shows no
   fixed floor set reaches `1`; that would make the rung ladder a dead end and
   redirect Route A2 back into the A1/A3 itinerary-rigidity problem.
