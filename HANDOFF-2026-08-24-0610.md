# HANDOFF 2026-08-24-0610

- **Branch:** `main`  **HEAD:** `eb3a51a`  **Tree:** clean
- **Build:** full `lake build` green, **8746 jobs**.  `src/` root-imported chain
  is sorry-free; one disclosed `sorry` lives in the not-yet-imported
  `FrontB/OneCircuit.lean` (active crux decomposition, see below).

## Done this session

### 1. Harmonic-dual obstruction project — COMPLETE (operator's scoped task)
Proved `no_positive_harmonic_local_certificate`
(`CollatzMoonshot/FrontA/BackwardHarmonicObstruction.lean`, imported, sorry-free):
the constant-lift-1 harmonic (`α=1`) local-certificate architecture on floors
`{1,7/4,3,6,12}` admits **no positive certificate at any depth `k ≥ 9`**.
Witness: a native-checked 65610-entry supersolution (`native_decide`, exact
contraction `0.99224 < 1`), the arbitrary-`x` child-residue lemma
`oddBlockChild_mod_19683` carrying depth-uniformity, and the Farkas maximizer
argument.  Axioms: `[propext, Classical.choice, Quot.sound]` + 4 finite
`native_decide` checks.  Experiment: `experiments/barrier_harmonic_dual.py`,
`experiments/harmonic_uniform_certificate.py`.  Full write-up in
`FRONT-A-HARMONIC-DUAL.md` (§Results, §8).  Honestly scoped: a no-go for ONE
certificate scheme, NOT a Collatz-divergence claim.

### 2. Front B ladder base — probe started (`FrontB/OneCircuit.lean`, 1 sorry)
Special-case-first on the real headline crux `NoNontrivialCycle ↔ FrontB`
(Route 2 `Compression`/`LadderCompletes`).  PROVED: closed forms for the
canonical one-circuit word `trueᵃfalseᵇ` (`numer = 3ᵃ−2ᵃ`, b-independent;
`den = 2^(a+b)−3ᵃ`), the identity `numer+den = 2ᵃ(2ᵇ−1)`, the reduction
`den ∣ numer ↔ den ∣ 2ᵇ−1`, and the **a=1 case** in full.  Disclosed `sorry`:
the a≥2 Böhm–Sontacchi core.

## Next steps (priority order)

1. **Close the `OneCircuit` a≥2 sorry** via PENDING_WORK.md path 1
   (interval-misses-power-of-2): `den ∣ 2ᵇ−1` + `den>0` pin `2ᵇ` to
   `(3ᵃ+1)/2ᵃ ≤ 2ᵇ ≤ (3ᵃ−1)/(2ᵃ−1)`, an interval with no power of 2 for a≥2.
2. Lift canonical → all 1-circuit words (`circuits v = 1 → rotation of
   `oneCircuitWord a b`); wire into the ladder base; then import from root.
3. Longer horizon: the repo headline rests on deep cited axioms
   (`Assumed/Cycles.lean` Eliahou/Hercher behind Baker; `Assumed/ABC.lean`;
   `Assumed/Tao2019.lean`).  See PENDING_WORK.md §"repo's real headline cruxes".

## Uncommitted edits
None — tree clean at `eb3a51a`.

## Commits this session
`d029483`→`ed87ab8`→`86bdfbf`→`6d0fa1b`→`d6437df`→`e8350a0`→`405973e`→`3b0fa4f`
(harmonic headline) →`8f3c52c`,`dad79fc` (docs) →`eb3a51a` (Front B probe).
