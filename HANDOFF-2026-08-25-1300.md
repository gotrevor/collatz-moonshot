# HANDOFF 2026-08-25-1300  (★ crux reduced to `b+d ≤ 5`; DECISIVE elementary lead open ★)

- **Branch:** `main`  **HEAD:** `4373eb8`  **Build:** 🟢 `lake build` 8751 jobs.  **Tree:** clean.
- **Read first, in order:** `DIRECTION.md` (CURRENT DIRECTIVE — outranks this; do NOT edit),
  `PENDING_WORK.md` (top block = live state, the MAJOR LEAD, and next steps), then this file.

## This run's arc (mandated paradoxical crux `le_two_blocks_not_acyclicParadoxical`)
Drove the interior two-block exclusion from an opaque residue `sorry` to the SINGLE inequality
`b + d ≤ 5`, proving everything else sorry-free, then found a likely-elementary route to that too.

### Proved sorry-free this run (`CollatzMoonshot/FrontA/Paradoxical.lean`)
- `near_critical_containment` — the sharpened crux: elementary squeeze `D·(2^d−1) < 3^(b+d)`,
  power bracket `2^m·(2^d−1) < 3^(b+d)·2^d`, tight window `3^(b+d) < 2^m < 2·3^(b+d)`, and the
  finite discharge (given `b+d ≤ 5`: `m ≤ 8` ⇒ ~800-case `interval_cases`+`omega`).
- `window_unique_m` — the window holds ≤1 power of 2 (`m` a function of `(b,d)`).
- Removed the unnecessary hypothesis `2 < n` from `two_block_residue_core` (Aristotle's note).

### THE sole `src/` sorry (the live crux) — `Paradoxical.lean:515`
    have hbd : b + d ≤ 5 := by sorry
Everything else in the two-block exclusion is machine-checked; headline
`finite_acyclicParadoxical_imp_noDivergent` stays `sorryAx`-free (audited this run).

## ★ NEXT LAP — THE #1 ATTACK (start here; likely closes the crux WITHOUT Baker) ★
Aristotle proved two ELEMENTARY sufficient conditions for `y ≤ n`:
  A: `3^(b+d) + 2^c·3^d < 2^m`      B: `(2^m − 3^(b+d))·(2^d − 1) ≥ 3^b·(3^d − 2^d)`
and then declared the residual `¬A ∧ ¬B` Baker-hard **without testing it**.  It is bounded:
**`¬A ∧ ¬B ∧ subcritical ∧ U₁>0 ∧ b,d≥1 ⟹ b+d ≤ 5`** — verified `b,d < 70` (`scratchpad/
resid2.py`, `elem_bound.py`), ZERO exceptions; BOTH negations essential (`H1` without `¬B` is
unbounded).  Elementary lever: `2^m = 2^(b+d)·2^(c+e) ≥ 2^(b+d)·2^c` (`e≥0`), so `¬A` gives
`2^c·(2^(b+d) − 3^d) ≤ 3^(b+d)` — a pure `2^g` vs `3^g` growth inequality.

**Plan (in order):**
1. Prove `le_of_gap_A` and `le_of_gap_B` (elementary; from `hI/hII` segment identities — Aristotle
   proved both sorry-free, so they are definitely provable; can mirror its ANALYSIS.md).
2. Prove `¬A ∧ ¬B ∧ subcrit ∧ U₁ ⟹ b + d ≤ 5`.  Attack: `by_contra` (`b+d ≥ 6`), then feed the
   exponent-atom inequalities to `nlinarith` with the growth facts `2^k ≤ 3^k` and the `¬A` lever.
   **OPEN RISK:** the contradiction may still need the tight (non-polynomial) `2^g`↔`3^g` relation;
   if `nlinarith` can't, the elementary route fails and we're back to Baker — so TEST THIS EARLY.
3. Route `two_block_residue_core` through `A ∨ B ∨ (¬A∧¬B ⇒ b+d≤5 ⇒ finite check)`, discharging the
   `b+d ≤ 5` sorry.  Then `le_two_blocks_not_acyclicParadoxical` is fully proved ⇒ classification
   PROMISING EVIDENCE → **GO** (a new infinite-family theorem strictly generalizing RT Appendix A).

If step 2 fails (Baker confirmed): the disclosed `b+d ≤ 5` sorry stays; do NOT relabel it as a
cited axiom to clear the src-sorry gate (games the anti-quit gate; a citation is BASELINE per
DIRECTION).  Narrow further via the 3-adic reframing (`L mod 3^b` / `orderOf 2 mod 3^b`).

## Independent validation (Aristotle job `4006e40e…`, DONE — do NOT resubmit)
It independently reproduced our EXACT reduction (same A/B, same Case A, same single residual) and
its exhaustive search (1.35M solutions; band to `g ≤ 119`) found no counterexample — strong
faithfulness evidence.  It only missed testing that `¬A∧¬B` is bounded (the lead above).

## Other fronts — UNCHANGED (do not touch without a new directive)
- Front A M2′ COMPLETE; parity-reconstruction COMPLETE/BASELINE; harmonic-dual COMPLETE.
- Front B `Compression` on hold (SdW source-blocked).  `OneCircuit` a≥2 = Steiner/Baker, off path.
