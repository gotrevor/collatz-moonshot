# Handoff: exponent-3/4 renewal+stopping pipeline complete

**Date**: 2026-08-24 01:31 UTC · **Branch**: main · **HEAD**: f0ebe41

## 🎯 What we're doing

Front A of the Collatz "moonshot": building an unconditional backward-tree
harmonic-growth frontier by climbing a ladder of endpoint-height exponents
(1/2 → 2/3 → 3/4 → … toward harmonic 1). Each rung needs (a) a Lean-checked
finite *local transfer certificate*, (b) a *renewal* layer (exact real edge
weight), and (c) a *stopping* recursion layer. This lap's scoped objective was
`sorry-free:CollatzMoonshot/FrontA/BackwardThreeQuartersStopping.lean` — the
final piece of the 3/4 rung. **Done.**

## 🧠 Context to carry forward

- **The whole ladder step is now a mechanical port.** The exponent-3/4 files are
  direct structural ports of the exponent-2/3 files. When you build the *next*
  rung, the templates are the trio `BackwardTwoThirds.lean` (certificate),
  `BackwardTwoThirdsRenewal.lean`, `BackwardTwoThirdsStopping.lean`. Port them
  the same way — rename identifiers, swap the constants, keep the proof skeleton
  byte-for-byte. Almost no real proof engineering was needed this lap.
- **Scale-up map 2/3 → 3/4** (the mechanical dictionary): source residue mod
  243→729, child/potential residue mod 81→243, shared digit `x/81%3`→`x/243%3`,
  edge weight `(52/25)(629/1000)^j`→`(2279/1000)(2973/5000)^j`, integer scale
  `25·1000^23`→`1000·5000^23`, underweight facts `(52/25)^3<9,4(629/1000)^3<1`
  (a **cube** comparison for 2/3) → `(2279/1000)^4<27,8(2973/5000)^4<1` (a
  **fourth-power** comparison for 3/4, because `((3/2^j)^(3/4))^4 = (3/2^j)^3`),
  potential ceiling 1051827→19334101, potential floor 100000→1000000,
  exponent `2/3`→`3/4`. Height floors `{1,7/4,5/2,3,4}`→`{1,7/4,3,6,12}`.
- **The exponent-N generalization of the underweight lemma**: for exponent p/q,
  compare `w^q < (3/2^j)^p`. For 2/3: `w^3 < (3/2^j)^2`. For 3/4:
  `w^4 < (3/2^j)^3`. The rpow bridge is `((3/2^j)^(p/q))^q = (3/2^j)^p` via
  `Real.rpow_natCast` + `Real.rpow_mul` + `norm_num` (checks `(p/q)*q=p`), then
  `lt_of_pow_lt_pow_left₀ q`.
- **Both min AND max potential fins are over `Fin 243`** for 3/4 (the potential
  depends on residue mod 243), whereas 2/3 used `Fin 81`. Don't copy the `81`.
- The state-independent value machinery (`StoppingChain`, `chain_agree`,
  `chain_reachesValuePos`, `round_add_le`, `reusable_band_step`,
  `weighted_biUnion_expands`, `reachesValuePos_self_iff_onCycle`,
  `reusableOddBlockChild_parent_eq`) is reused **verbatim** from
  `BackwardStopping.lean` — it only touches ℕ values, never state labels.
- `ThreeQuartersNode` alias lives in `...Renewal.lean` now (relocated out of the
  stopping module, which was allowed). Keep new aliases in the renewal layer.

## ✅ State

- `BackwardThreeQuartersRenewal.lean` (new) and
  `BackwardThreeQuartersStopping.lean` (completed): both **sorry-free**, imported
  from `CollatzMoonshot.lean`.
- Full `lake build` **green, 8,742 jobs** — observed this session and via the
  pre-commit hook on commit **f0ebe41**.
- Axiom audit (real output this session) on `exists_threeQuartersChildFinset`,
  `threeQuartersRepeatOrStoppingGrowth`, `threeQuartersStoppingFrontier_of_noCycle`,
  `threeQuarters_stopping_card_bound`: base three `[propext, Classical.choice,
  Quot.sound]` plus only the allowed native_decide certificate axioms
  (`threeQuartersNatCertificate._native…`, `…_pos_of_unit._native…`, and for the
  final theorem also `…_ge_min_fin._native…`, `…_le_max_fin._native…`).
  **No project axioms, no sorry.**
- Pinned `threeQuarters_stopping_card_bound` and its constants `1000000`/
  `19334101` preserved verbatim.
- Committed f0ebe41. `box done --green` signalled (scoped target met). Treadmill
  stop requested — will NOT relaunch.

## 🎬 Next actions

1. If continuing the ladder: pick the next exponent rung (e.g. 4/5 or a
   correlated deeper-memory operator) via `experiments/barrier_transfer.py`,
   freeze the certificate, then port the trio using the scale-up map above.
2. **But note the real open math is NOT another rung** — it's step 7/8 of
   `FRONT-A-ROUTES.md`: uniform vs. pointwise harmonic growth (the 3-adic
   adversary, which is already pushing the measured slope *down*), and the
   annular overlap/packing lemma. Every rung stays `< 1`; more rungs alone never
   reach harmonic. Decide experimentally before formalizing more rungs.

## ⚠️ Gotchas

- `set_option maxRecDepth 10000` is **required file-wide** in the renewal module
  (and set in stopping too): the 810-entry `threeQuartersWeights` array trips the
  default recursion depth inside `simp [threeQuartersRationalWeight]` and
  `exact_mod_cast …_le_max`. First build failed without it.
- `native_decide` is deliberately used for the 3/4 finite potential bounds
  (array scale ≈10^85); `#print axioms` showing the native axiom is expected and
  allowed (operator instruction).
- All `threeQuarters*` identifiers share the namespace `CollatzMoonshot.FrontA`
  with the 2/3 names — keep the `threeQuarters` prefix on everything new.

## 📁 Key files

- `CollatzMoonshot/FrontA/BackwardThreeQuartersRenewal.lean` — new renewal layer.
- `CollatzMoonshot/FrontA/BackwardThreeQuartersStopping.lean` — this lap's target.
- `CollatzMoonshot/FrontA/BackwardThreeQuarters.lean` — the frozen 3/4 certificate.
- `CollatzMoonshot/FrontA/BackwardTwoThirds{,Renewal,Stopping}.lean` — port templates.
- `CollatzMoonshot/FrontA/BackwardStopping.lean` — reused value machinery.
- `FRONT-A-ROUTES.md` — the exponent ladder / route map (step 6 marked DONE).

---
**→ Next session: this is your starting point. Don't summarize this doc back to
Trevor or wait for instructions, and don't offer other projects from the KB —
this doc IS the chosen thread. Absorb the context above; the 3/4 rung is DONE and
committed, so the honest next move is to weigh a further rung against the real
open math (uniform harmonic growth / packing, step 7/8 of FRONT-A-ROUTES.md)
before grinding another mechanical port.**
