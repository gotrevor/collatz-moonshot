# Handoff: NetHalfRepeatOrStoppingGrowth proved — Front A stopping line closed

**Date**: 2026-08-23 20:27 UTC · **Branch**: main · **HEAD**: `49ed0db` (green, observed)

## 🎯 What we're doing
Front A (no divergent Collatz orbit), Route A2's barriered-backward-tree ladder in
`FRONT-A-ROUTES.md`. This lap's scoped objective — prove the pinned recursive theorem
`NetHalfRepeatOrStoppingGrowth` in a new `CollatzMoonshot/FrontA/BackwardStopping.lean`
— is **done**: sorry-free, `lake build` green (pre-commit hook re-verified),
`#print axioms` = `[propext, Classical.choice, Quot.sound]` (observed in real output).
The run signalled `box done --green`; the treadmill will not relaunch.

## 🧠 Context to carry forward
- **Proof shape** (the part not obvious from the file): a fuel-`H+1` frontier recursion
  from `(false, d)`. Each node carries a `StoppingChain` — an odd-parent ancestor chain
  whose *length equals its creation round*. Because `3y+1 = 2^j·(odd)` determines the odd
  parent, `chain_agree` forces two chains from a common value to agree pointwise; so a new
  endpoint colliding with a *live frontier value* (chains of different lengths ⇒ `d`
  recurs mid-chain) or with *its own ancestry* splices via `chain_reachesValuePos` into
  `ReachesValuePos n n = OnCycle n`, `d ≤ n`. This localizes the global "repeat ⇒ cycle"
  argument that otherwise needs a whole-tree depth function. Termination:
  `StoppingChain.round_add_le` (chain values are `m` distinct elements of `Icc d H`, so
  `m + d ≤ H + 1`). Mass: `weighted_biUnion_expands` with exact `sqrt(parent/child)`
  telescoping; exits frozen by sum-splitting over `filter (·.2 ≤ H)`.
- **Interface repair made**: `exists_netHalfChildFinset` (BackwardRenewal) had erased the
  cost bound `j ≤ 23`; I strengthened it and its two private constructors with the clause
  `3c < 2^23·x`. That clause feeds the `2^23 H` exit bound and, through
  `reusable_band_step`, the `2^25 H` band ceiling. Target `def`s were NOT altered.
- **What this buys downstream** (already wired, no new work needed): with
  `NetHalfStoppingFrontier.card_bound`, every odd unit `2 ≤ d ≤ H` unconditionally has
  either a positive cycle at/above `d`, or `> V(d)·√(H/d)/200` distinct first-exit
  preimages in `(H, 2^23 H)` with paths inside `[d, 2^25 H]`. Ladder step 4 closed.
- **What remains open (the real math)**: step 5 — decide pointwise vs. uniform harmonic
  growth (the 3-adic adversary in `experiments/barrier_adversary.py` drives the uniform
  constant down: `0.2195 → 0.1555` by `p=30`); and step 6 — `DivergentTailHarmonicBudget`
  + annular overlap/packing. Do NOT treat the √-exponent as harmonic growth; numerics cap
  the net-height critical exponent below `1` (≈`.78` at depth 7).

## ✅ State
- `49ed0db` on `main`: `BackwardStopping.lean` added, imported from `CollatzMoonshot.lean`;
  `BackwardRenewal.lean` child interface strengthened; HANDOFF.md + FRONT-A-ROUTES.md
  updated. Working tree clean. Not pushed (no egress; host pushes).
- Axiom audit observed: `netHalfRepeatOrStoppingGrowth`, `stoppingFront_step`,
  `exists_netHalfChildFinset` all base-three only.

## 🎬 Next actions
1. Read `FRONT-A-ROUTES.md` §"chippable mathematical ladder" — the crux is now step 5.
   Sharpen it experimentally first: extend `barrier_adversary.py` to the *net-height*
   certificate states (mod-27 grid with the `j=1` branch) and test whether the 3-adic
   adversary also degrades the exponent-`1/2` telescoping mass, or only the harmonic slope.
2. If the `1/2`-mass survives adversarial seeds, pin (as a `def`, per repo doctrine) a
   uniform √-growth statement and start the moving-seed overlap question; if it degrades,
   record the refutation in FRONT-A-ROUTES.md and re-price A2 vs A1.
3. Alternative thread if A2 stalls: `frontB_of_compression_le_91` (Front B compression on
   primitive words) is the standing sharpened target on the other board.

## ⚠️ Gotchas
- Current toolchain: `push_neg` deprecated → use `push Not`; the sum split is
  `Finset.sum_filter_add_sum_filter_not`; `add_lt_add_right hexp _` elaborated with the
  constant on the LEFT here — `add_lt_add_of_lt_of_le hexp le_rfl` is the safe form.
- `NetHalfChildAt d (s.1, s.2) c` vs `NetHalfChildAt d s c`: Prod eta is definitional;
  plain `exact` bridges them.
- Don't create a `HANDOFF.md` symlink; the repo scheme is dated files + a master
  `HANDOFF.md` updated in place (its top now carries this lap's update section).

## 📁 Key files
- `CollatzMoonshot/FrontA/BackwardStopping.lean` — the new theorem (this lap's product)
- `CollatzMoonshot/FrontA/BackwardRenewal.lean` — targets/defs + strengthened child finset
- `FRONT-A-ROUTES.md` — route map; ladder status current as of this lap
- `experiments/barrier_adversary.py`, `experiments/barrier_stopping.py` — the numeric probes for step 5

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or
wait for instructions, and don't offer other projects from the KB - this doc IS the chosen
thread. Absorb the context above, then pick up at "Next actions" #1 and start working.**
