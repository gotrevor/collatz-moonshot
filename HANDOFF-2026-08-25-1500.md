# HANDOFF 2026-08-25-1500  (★ review lap: elementary route for `b+d ≤ 5` REFUTED; crux is Baker-grade ★)

- **Branch:** `main`  **Build:** 🟢 `lake build` 8751 jobs.  **Tree:** clean at commit time.
- **Read first, in order:** `DIRECTION.md` (CURRENT DIRECTIVE — updated this lap, OUTRANKS this;
  do NOT edit on a grind lap), `PENDING_WORK.md` (top block = live state + the REFUTATION), then this.

## What this (review) lap did
Fresh-mind course-correction. The crux `le_two_blocks_not_acyclicParadoxical` is machine-checked
modulo the single inequality `b + d ≤ 5` (sole `src/` sorry, `Paradoxical.lean:515` inside
`near_critical_containment`). The previous baton queued a "MAJOR LEAD": prove `b+d ≤ 5` ELEMENTARILY
via Aristotle's criteria A/B (the claimed GO upgrade, no Baker). **This lap decisively REFUTED that.**

### The refutation (decision-changing)
A `nlinarith`/polynomial certificate is a Positivstellensatz combination valid for all REAL atom
values. I proved the **real relaxation of `¬A∧¬B∧subcrit∧U₁ ⇒ b+d≤5` is FEASIBLE at unbounded `g`**:
- Fully-checked exact-rational witness at `b=18, d=23` (`g=41`): real `C=2^c≈8.3·10⁶`, `V=2^m≈3.6·10¹⁹`
  satisfy ALL of `{subcrit, ¬A, ¬B, U₁, lever 2^m≥2^(b+d)·2^c, couplings 3^(3d)≤2^(5d) & 3^(3b)≤2^(5b),
  size 2^g≥64 & 3^g≥729}`. Feasible at every even `g∈[6,60]`. See `experiments/two_block_relaxation.py`.
- Cross-checked in Lean (transient `wip/BdTest.lean`, since deleted): the coupling `have`s all
  compile; `nlinarith` with the full curated hint set fails (`linarith failed`) — exactly as the
  relaxation predicts.
- Integer truth is unaffected/re-confirmed: `¬A∧¬B∧subcrit∧U₁ ⇒ b+d≤5` holds over actual powers to
  `g≤200`, exactly 4 pairs `(b,d)∈{(2,1),(2,3),(3,2),(4,1)}`. The prior lead's error was conflating
  "true over integer powers" (integrality-forced) with "nlinarith-provable" (needs a real certificate).

**Why real-feasible but integer-empty:** the window `2^m/3^g ∈ (1, 2^d/(2^d−1))` has width `~2^-d`;
an integer solution needs `|m·log2 − g·log3|` tiny = effective irrationality of `log₂3` (Baker). At
`g=41`, closest is `2^65/3^41≈1.0117` ≫ the `~2^-23` window; only `g=5` fits. **`b+d ≤ 5` is genuinely
Baker-grade** — matches Aristotle's independent linear-forms-in-logs verdict and the earlier `dbig.py`
"real bound infinite" finding.

### Axiom audit (re-run this lap)
- `finite_acyclicParadoxical_imp_noDivergent`: `[propext, Classical.choice, Quot.sound,
  rozier_terracol_3_2]` — clean, one faithful cited axiom, no `sorryAx`.
- `parityRigidityW1'_imp_noDivergent`: trust base only.
- `le_two_blocks_not_acyclicParadoxical`: `[propext, sorryAx, Classical.choice, Quot.sound]` — the
  single disclosed crux sorry (`b+d ≤ 5`).

## ★ NEXT LAP — the ONLY live path to GO ★
Build an explicit **effective irrationality / linear-forms-in-logs bound for `log₂3`** in Lean.
Concretely, a statement sufficient to close the window: `∀ m k, k ≥ 6 → 3^k / 2^d_bound < |2^m − 3^k|`
— or more usefully `∃ (C κ), ∀ m k, |2^m − 3^k| ≥ 3^k · C / k^κ` with explicit `C, κ`, from which the
window (`hbracket`/`hupper` already proved in `near_critical_containment`) forces `b+d ≤ 5` and the
existing finite discharge finishes. Mathlib has ONLY `LiouvilleWith` (no specific effective measure).
Steps to scope next lap:
1. Search mathlib for any usable partial: `Nat.log`, `Real.logb`, continued-fraction API,
   `Irrational`/`LiouvilleWith`, Baker-adjacent lemmas. (Likely nothing effective → must build.)
2. Decide granularity: the SMALLEST sufficient effective bound (only `k ≥ 6` matters here, and `d` is
   coupled to `k` by ¬B, so a modest gap may suffice). A finite CF-convergent argument for `log₂3`
   giving `|2^m − 3^k| ≥ 3^k · 2^{-α k}` with `α < 1` is enough; that IS provable (CF theory) and is
   the realistic multi-lap GO target.
3. If genuinely out of reach this lap: the disclosed `b+d ≤ 5` sorry STAYS (do NOT relabel it a cited
   axiom to clear the gate — that is BASELINE per DIRECTION and games the anti-quit gate). Narrowing
   the effective bound (even stating the exact sufficient lemma + its CF proof sketch) is a valid lap.

## FORBIDDEN (refuted / off-path — see DIRECTION)
- Any elementary `nlinarith`/`omega` bound on `b+d` / `d` (real relaxation feasible → no certificate).
- The `le_of_gap_A/B` + `¬A∧¬B⇒b+d≤5` plan from the old baton — void.
- Front B `Compression` (source-blocked), `OneCircuit` a≥2 (Steiner/Baker, isolated), M2′ rebuild,
  more harmonic exponents, parity-reconstruction plumbing.

## Other fronts — UNCHANGED
Front A M2′ COMPLETE; parity-reconstruction COMPLETE/BASELINE; harmonic-dual COMPLETE. Front B on hold.
