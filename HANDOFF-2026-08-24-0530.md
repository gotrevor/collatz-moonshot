# HANDOFF 2026-08-24-0530 — harmonic-dual obstruction project COMPLETE

- **Branch:** `main`  **HEAD:** `3b0fa4f`  **Tree:** clean
- **Build:** full `lake build` green, **8746 jobs**, no `sorry`, no project axioms.

## What this lap delivered (the operator's scoped project, done)

Executed the full harmonic-dual obstruction project end-to-end and **proved the
headline in Lean, sorry-free**.

### Experiment (reproducible, exact)
`experiments/barrier_harmonic_dual.py` (fixed/min policies, exact rational
left-dual verifier, finite-memory supersolution mode) +
`experiments/harmonic_uniform_certificate.py` (freezes the integer certificate,
emits the Lean array).  Findings, all recorded in `FRONT-A-HARMONIC-DUAL.md`:

- Reproduces every recorded const-1 radius (depths 2–10) and min radius exactly.
- Refining floors / adding children leaves the const-1 radius invariant; only
  **raising the ceiling** pushes it toward 1 (.933 at cap 12 → .984 at cap 3072).
  The obstruction is specific to a fixed bounded floor set.
- Floor-only supersolution fails (c=1.51).  The finite-memory `(r mod 3^m,floor)`
  supersolution value `c*(m)` decreases to 1 from above, crossing below **only at
  m=9**: exact `c = 0.99224 < 1`, 0 violations over all 196830 depth-10 states.
- Compression **refuted**: digit-invariant, not rank-1 (128× spread), not
  floor-only, not low-memory.  The uniform gap is real but structureless.

### Lean (`CollatzMoonshot/FrontA/BackwardHarmonicObstruction.lean`, imported)
- `harmonicNatCertificate` — `native_decide` over all 196830 states
  (`Fin 5 × Fin 59049`): the const-lift-1 harmonic operator contracts the frozen
  65610-entry weight `W`, exact ratio `0.99224 < 1`.
- `oddBlockChild_mod_19683` — child residue mod `3^9` from source mod `3^10` for
  **arbitrary x** (fully proved): this is the depth-uniformity — one finite check
  governs every depth `k ≥ 9`.
- `harmonicQImage_lt` (rational `M W < W`), `harmonicImageOfAt_smul`/`_mono`,
  child-closure lemmas, `no_positive_subsolution_of_supersolution` (abstract
  Farkas/maximizer core).
- **`no_positive_harmonic_local_certificate`** — the headline: no positive weight
  satisfies the const-lift-1 harmonic local-expansion inequalities at any unit
  residue, hence at any depth `k ≥ 9`.  Axioms `[propext, Classical.choice,
  Quot.sound]` + 4 `native_decide` finite checks.

## Scope / honesty guardrail
This is a no-go **for one certificate architecture** (constant lift digit, fixed
floor set `{1,7/4,3,6,12}`), NOT a statement about Collatz divergence.  It proves
that this particular uniform finite-state local-potential scheme cannot reach
harmonic exponent `α = 1` — matching the numerical evidence that the 3-adic
adversary drives the achievable exponent below 1.

## Commits this lap
`d029483` experiment+memory-9 discovery · `ed87ab8` integer cert+compression
refuted · `86bdfbf` native certificate module · `6d0fa1b` dual lemma · `d6437df`
rational supersolution · `e8350a0` generic operator+child lemmas · `405973e`
pos/smul/mono · `3b0fa4f` **headline proved**.

## Next (new direction; the scoped project is finished)
The harmonic rung is closed.  Open Front-A mathematics is unchanged: steps 5–6 of
`FRONT-A-ROUTES.md` (uniform vs. pointwise harmonic growth on the *full* barriered
tree; overlap/packing), and the A1/A3 itinerary-rigidity problem.  The harmonic
no-go says the local-certificate ladder alone cannot reach `α=1`, which redirects
Route A2 back toward those.
