# Handoff: binaryBarrierSubtreeGrowth proved — Front A ladder step 1 done

**Date**: 2026-08-23 17:12 UTC · **Branch**: main (HEAD `91f64be`)

## 🎯 What we're doing

Front A (no divergent orbit), Route A2 chippable ladder from `FRONT-A-ROUTES.md`. This
scoped run's objective was exactly `sorry-free:CollatzMoonshot/FrontA/BackwardBranching.lean`,
i.e. iterate the proved two-child arithmetic kernel `exists_two_unitOddBlockChildren` into
the finite `2^r` floor-preserving subtree theorem `binaryBarrierSubtreeGrowth`. **Objective
met; `box done --green` signalled.**

## 🧠 Context to carry forward

- Proof architecture (all in `BackwardBranching.lean`):
  - `reachesInBand_trans`: band witnesses compose — path `y→m` in `[m,Y]` then `m→d` in
    `[d,X]` gives `[d,Z]` for any `Z ≥ max(X,Y)`; split the iterate index at the junction
    with `Function.iterate_add_apply`.
  - `unitOddBlockChild_parent_eq`: children of distinct **odd** parents can't collide —
    `3y+1 = 2^j x = 2^{j'} x'` with `x,x'` odd forces `j = j'` (else the smaller-exponent
    parent is even), hence `x = x'`. Asymmetric core is `parent_eq_of_exp_le`.
  - `exists_binary_level`: `Nat.le_induction` from level 1; members of each level are odd
    units, so the collision lemma applies; children chosen via `choose` on `S.attach`,
    level doubled with `Finset.biUnion` + `Finset.card_biUnion` (disjointness from parent
    uniqueness), each child's `[m,128m]` block lifted by `reachesInBand_trans`.
  - Levels `r ≥ 1` carry the extra invariant `m % 2 = 1`; the public statement drops it
    and handles `r = 0` with the singleton `{d}`.
- Oddness of level members is load-bearing: the collision argument needs odd parents, and
  level 0 (`d` itself, possibly even) has only one member so no collision arises there.
- Statement was NOT weakened and no axioms added — audit observed in real output:
  `binaryBarrierSubtreeGrowth` depends on `[propext, Classical.choice, Quot.sound]` only;
  the collision and composition lemmas don't even need `Classical.choice`.

## ✅ State

- `CollatzMoonshot/FrontA/BackwardBranching.lean` sorry-free; imported (with
  `BackwardTree`) from `CollatzMoonshot.lean`.
- `lake build` green (observed: "Build completed successfully (8732 jobs)", also re-run by
  the pre-commit hook at commit time).
- Commits this session: `5e62df9` (the proof + docs), `91f64be` (pycache cleanup +
  `.gitignore`). Not pushed (no egress; host pushes).
- `HANDOFF.md` (master) and `FRONT-A-ROUTES.md` updated: ladder step 1 marked DONE.

## 🎬 Next actions

1. Nothing owed on the scoped objective — the run is complete and stop is signalled.
2. If a new lap opens on this repo anyway: the next genuine-math attack is ladder step 2
   in `FRONT-A-ROUTES.md` — replace the crude `2 children / ×128` certificate by a
   residue-and-height transfer certificate that includes the shrinking `j=1` branch above
   the floor (goal: improve the counting exponent `1/7`).
3. Alternative threads: pointwise-vs-uniform harmonic growth decision (the 3-adic
   adversary in `experiments/barrier_adversary.py` is the falsification side), and Front B's
   sharpened `frontB_of_compression_le_91`.

## ⚠️ Gotchas

- This toolchain rejects bare `le_or_lt` in that context — `Nat.lt_or_ge` worked.
- Don't `rw` an equation like `j'-j = (j'-j-1)+1` when the goal contains `j'-j-1`: the
  rewrite loops into the subterm. The clean move for "odd number can't be `2^k·x`, k≥1"
  is a `2 ∣ x` derivation via `dvd_pow_self` + `omega`, not `ring` normalization.
- `obtain` on a hypothesis clears it — save any dot-notation consequence
  (`h.reachesInBand`, `h.self_mem_band`) *before* destructuring.

## 📁 Key files

- `CollatzMoonshot/FrontA/BackwardBranching.lean` — the completed theorem + helpers.
- `CollatzMoonshot/FrontA/BackwardTree.lean` — `ReachesInBand`, spine theorems, growth pins.
- `FRONT-A-ROUTES.md` — route map; ladder step 2 is the next attack.
- `HANDOFF.md` — master state (already refreshed this session).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or
wait for instructions, and don't offer other projects from the KB - this doc IS the chosen
thread. The scoped run is DONE; if relaunched on this repo, absorb the context above and
start at "Next actions" #2 (the residue-and-height transfer certificate).**
