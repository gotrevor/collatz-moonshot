# HANDOFF 2026-08-25-1900  (★ crux fully reduced to its irreducible Baker core; sharp CF engine built ★)

- **Branch:** `main`  **HEAD:** `09e00af`  **Build:** 🟢 `lake build` 8752 jobs.  **Tree:** clean.
- **Read first:** `DIRECTION.md` CURRENT DIRECTIVE (GO route; do NOT edit), `PENDING_WORK.md` top
  (full inventory + finished-proof structure), then this.

## What landed this lap (17 green commits, all in `CollatzMoonshot/FrontA/PowSeparation.lean`)

The sole `src/` crux sorry is unchanged: `sep_two_three` (`3^(3k) ≤ (2^m−3^k)^3·2^k` near-critical,
i.e. `|2^m−3^k| ≥ 3^k·2^(−k/3)`).  Everything ELSE around it is now machine-checked (trust base only).
This lap built the **complete elementary reduction of `sep_two_three` to its single irreducible
Baker input**, from scratch (mathlib has no CF best-approximation lower bound, no effective measure,
no Baker):

- **Reduction routes:** `sep_of_linear_form` (→ Baker linear form `Λ=m·log2−k·log3 ≥ 2^(−k/3)`),
  `sep_of_measure` (→ integer irrationality measure `3^k ≤ (2^m−3^k)·k^C`), `sep_of_logb_gap`
  (→ closeness of `m/k` to `log₂3`).
- **Best-approximation engine:** `irrational_logb_two_three`; bridge `lt_logb_two_three_iff` /
  `logb_two_three_lt_iff` (`a/b ⋛ log₂3 ↔ 2^a ⋛ 3^b`, decidable); `denom_ge_of_between` (unimodular
  denominator gap, pure ℤ); `theta_dist_lower` (min-gap) and **`theta_dist_lower_sharp`** (tight
  `‖q_nθ‖`-quality bound `|kθ−p| ≥ min(bθ−a, c−dθ)`, via the ring identity
  `kθ−p = (kc−pd)(bθ−a)+(ka−pb)(c−dθ)`); `linForm_dist_lower`.
- **Wiring / CF algebra:** `linear_form_eq_logb`, `convergent_det_step` (unimodularity propagates),
  `convergent_denom_grow`.
- **Capstones:** `sep_of_bracket` and **`sep_of_bracket_sharp`** — per-`k` interface: a straddling
  unimodular bracket `a/b<log₂3<c/d` (`k<b+d`) with `2^(−k/3) ≤ log2·min(bθ−a, c−dθ)` ⇒ `sep`.
- **Base data:** `logb_bracket_19_12_65_41` (verified consecutive convergents, det=−1, straddle k<53).

**Independently corroborated by an Aristotle attempt:** identical reduction; residual is
irreducibly effective-linear-forms-in-two-logs (Baker).  Alt routes REFUTED (see
`FRONT-A-PARADOXICAL.md` addendum): CF-alone, base-3 `|2^p−3^q|≥1`, Padé (caps `2^{m/2}`), combining
1-D measures (needs `(μ₁−1)(μ₂−1)<1`, impossible).

## ★ NEXT LAP — the finished-proof structure (in PENDING_WORK.md) ★
`sep_two_three` = (finite `native_decide`, `6≤k≤K₀`) ∪ (large `k`: pick the convergent bracket
`(q_n,q_{n+1})` straddling `k`, apply `sep_of_bracket_sharp`; gap clears BECAUSE `a_{n+1} ≲ 2^{q_n/3}`).
Three remaining build items:
  (i) the `log₂3` convergent sequence as `native`-verified `2^a⋛3^b` data + the recurrence
      (`convergent_det_step` gives unimodularity);
  (ii) the "straddling convergent exists" selection lemma (CF bookkeeping, not Baker);
  (iii) the sub-exponential partial-quotient bound `a_{n+1} ≲ 2^{q_n/3}` for `log₂3` — the sole
       irreducible Baker content.  Prove = GO; cite as axiom = BASELINE (do NOT use to clear the gate).
Items (i)-(ii) are elementary multi-lap infrastructure; (iii) is genuinely multi-month (no mathlib
support).  Small-`k` finite check verified to `k<500` (here + Aristotle).

## Other fronts — UNCHANGED
Front A M2′ COMPLETE; parity-reconstruction COMPLETE/BASELINE; harmonic-dual COMPLETE. Front B on hold.
Classification of the paradoxical project unchanged: **PROMISING EVIDENCE** (crux open behind a
precisely-pinned, independently-corroborated Baker input).

## Axiom audit
`le_two_blocks_not_acyclicParadoxical` = `[propext, sorryAx (=sep_two_three), Classical.choice,
Quot.sound, finite_two_block_check native_decide ax]`.  All new lemmas this lap are trust-base only
(no `sorry`, no new axiom).  `finite_acyclicParadoxical_imp_noDivergent` unchanged (`+
rozier_terracol_3_2`).
