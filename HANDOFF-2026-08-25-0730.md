# HANDOFF 2026-08-25-0730  (★ two-block exclusion crux reduced to ONE finite containment ★)

- **Branch:** `main`  **HEAD:** `3bfa0b3`  **Tree:** clean  **Build:** 🟢 `lake build` 8751 jobs.
- **Read first, in order:** `DIRECTION.md` (CURRENT DIRECTIVE — outranks this file; do NOT edit),
  `PENDING_WORK.md` (top block = live state + next attacks), then this file.
- **Uncommitted edits:** none. All work committed.

## What this run did (fresh-mind review lap + grind)

Executed `DIRECTION.md`'s mandated move — the paradoxical project's new-mathematics crux, the
interior two-block exclusion `le_two_blocks_not_acyclicParadoxical` (strictly generalizes
Rozier–Terracol Appendix A). Drove it from **one opaque `sorry`** to **one sharply-stated
finite containment `sorry`**, proving everything else along the way.

### Landed sorry-free this run (`CollatzMoonshot/FrontA/Paradoxical.lean`)
- **Case A** (both blocks subcritical): `y ≤ X ≤ n` via two `headBlock_endpoint_le`, with the
  itinerary split at step `b+c` (`traceWord_add` + `List.append_inj`).
- **`two_block_residue_core`** — the whole exclusion reduced to ONE self-contained ℕ lemma
  (segment identities `2^(b+c)X+2^b = 3^b(n+1)`, `2^(d+e)y+2^d = 3^d(X+1)` + subcriticality
  + `n>2` ⇒ `y ≤ n`). Derived both identities in-kernel from `tstep_iterate_identity`.
- **`core_of_gap`** — the sharp elementary regime. Division-free `∀`-form gap
  `∀ w, 2^(c+d)−2^c+1 ≤ 3^b·w → U₁ < D·w` (`U₁=3^d(2^c−1)−2^(c+d)`, `D=2^m−3^(b+d)`),
  instantiated at the TRUE `w₁` (`3^b·w₁=2^c·X+1`, `2^b∣n+1`), contradicting the contrapositive
  `D·w₁ ≤ U₁` (from the exact identity `2^m·y+2^(b+c+d)=3^(b+d)(n+1)+3^d·2^b(2^c−1)` under `n<y`).
  Uses `maxHeartbeats 800000`.
- **`residue_core_exc1`, `residue_core_exc2`** — the two exceptional tuples `(2,3,3,0)`,
  `(3,3,2,0)`, closed by `omega` (their identities are concrete linear systems).

### Key discovery (exhaustive, `scratchpad/*.py`)
Using the **integer** ceiling bound on `w₁` (not the real-valued `t/3^b`), the sharp gap holds
for **every** subcritical `(b,c,d,e)` **except exactly two**: `(2,3,3,0)` and `(3,3,2,0)`
(checked `b,c,d,e < 34`, count stable). The contrapositive `D·w₁ ≤ U₁` + `w₂≥1` closes ~99.7%
even with the real bound; the integer ceiling closes all but 2.

## The SOLE remaining `src/` sorry (the live crux) — `Paradoxical.lean:418`
Inside `two_block_residue_core`, the `¬(sharp gap)` branch. What remains is precisely the
**finiteness containment**:

    ¬(sharp gap) ∧ U₁ > 0  →  (b,c,d,e) ∈ {(2,3,3,0),(3,3,2,0)}

Once proved, the branch closes immediately: `rcases` the two tuples and `exact
residue_core_exc1/2 …` (both already proved).

**Why it's still nontrivial (established this run):** crude inequalities only give the
near-critical strip `2^(m−1) < 3^(b+d) < 2^m` (`c+e ≈ 0.585(b+d)`), which is INFINITE. The
reason only 2 tuples fail is that the ceiling `⌈t/3^b⌉` grows along the strip — a
`3^b mod 2^(c+d)` least-residue fact. So the containment carries the same least-residue
hardness as before, just repackaged onto a FINITE target. Narrow it lap by lap; do not file
infeasible.

**Next concrete attacks (priority order):**
1. Prove `¬gap → b+d ≤ 5` via ceiling discreteness (`3^b·⌈t/3^b⌉ < t + 3^b`) plus a
   least-residue growth bound, then `interval_cases` on the small params + `decide`/`omega`.
2. A `ZMod (3^b)` least-residue lemma: minimal `w₁` with `3^b·w₁ ≡ 1−2^c (mod 2^(c+d))` is
   large enough (`≥ R'/D`), giving the containment (or closing the residual directly).
3. An **Aristotle** attempt is working the full self-contained
   core async (~7% at ~32 min, exploring the same structure numerically). If it returns a
   clean proof: kernel-verify with `#print axioms` (must be trust-base clean, no `sorryAx`/new
   axiom) BEFORE trusting, then inline it to supersede the residual sorry. Do NOT resubmit.

## Other fronts — UNCHANGED (do not touch without a new directive)
- Front A M2′ COMPLETE; parity-reconstruction COMPLETE/BASELINE; harmonic-dual COMPLETE.
- Front B `Compression` on hold (SdW source-blocked). `OneCircuit` a≥2 = Steiner/Baker, off
  path (`SteinerOneCircuit` hypothesis). Do NOT rebuild any of these.

## Axiom footprint
No new axioms this run. `two_block_residue_core` (hence `le_two_blocks_not_acyclicParadoxical`)
carries `sorryAx` from the one disclosed containment sorry; it is standalone discovery, NOT
wired into any headline (`finite_acyclicParadoxical_imp_noDivergent` stays trust base +
`rozier_terracol_3_2`, no `sorryAx`).
