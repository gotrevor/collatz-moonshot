# Handoff: exponent-2/3 recursive stopping-line theorem landed

**Date**: 2026-08-23 23:23 UTC · **Branch**: main · **HEAD**: 9c1d838

## 🎯 What we're doing

Front A, Route A2: the barriered backward-tree pull. This lap discharged the last
pinned `def` of the exponent-`2/3` renewal layer,
`TwoThirdsRepeatOrStoppingGrowth`, into a real theorem. This was the flagged
"Next" crux from the prior handoff and is now DONE, unconditional, axiom-clean.

## 🧠 Context to carry forward

- The exponent-`2/3` layer mirrors the exponent-`1/2` ("netHalf") layer one-for-one.
  The half layer's `netHalfRepeatOrStoppingGrowth` lives in `BackwardStopping.lean`;
  I ported it to five states. The KEY structural insight that made this a ~1-shot
  port: the entire ancestor-chain / collision-to-cycle / termination / band-step
  machinery in `BackwardStopping.lean` is **value-based** (operates on `ℕ`
  endpoints, never on the `Bool`/`Fin 5` state label). So `StoppingChain`,
  `consChain`, `chain_agree`, `chain_reachesValuePos`, `StoppingChain.round_add_le`,
  `reusable_band_step`, `ReusableOddBlockChild.lt_of_le`,
  `reachesValuePos_self_iff_onCycle`, and `weighted_biUnion_expands` are all reused
  VERBATIM by just importing `BackwardStopping`. Only the state wrappers and three
  round lemmas needed re-derivation.
- Three concrete deltas from the half version to watch if you touch this file:
  1. `twoThirdsPotential : Fin 5 → ℕ → ℕ` is **ℕ-valued** (netHalfStatePotential
     was ℝ-valued). Every real sum therefore carries an explicit `(· : ℝ)` cast.
  2. `InFiveHeightState` is a **single inequality**, not a conjunction — use
     `InFiveHeightState.le` (gives `d ≤ x`) where the half proof wrote `.1`.
  3. `exists_twoThirdsChildFinset` takes an **extra `hd : 2 ≤ d`** argument that
     `exists_netHalfChildFinset` did not.
- The root state is `(0 : Fin 5, d)`; floor 0 has num/den `1/1` so
  `InFiveHeightState d 0 d` is trivially `1*d ≤ 1*d` (`inFiveHeightState_root`).
- Faithfulness: `TwoThirdsRepeatOrStoppingGrowth` (the Prop `def`) and
  `TwoThirdsStoppingFrontier` were left exactly as pinned in
  `BackwardTwoThirdsRenewal.lean` — not weakened, not redefined.

## ✅ State

- `CollatzMoonshot/FrontA/BackwardTwoThirdsStopping.lean` — new, **sorry-free**.
  Proves `twoThirdsRepeatOrStoppingGrowth`, plus `twoThirdsStoppingFrontier_of_noCycle`
  (NoNontrivialCycle wiring killing the cycle alternative),
  `twoThirdsPotential_ge_min` (kernel-checked `100000 ≤ potential` on unit residues),
  and `twoThirds_stopping_card_bound` (explicit corollary).
- Imported from `CollatzMoonshot.lean`. **Full `lake build` green — 8,739 jobs**
  (observed this session, twice: standalone + pre-commit hook).
- Axiom audit (observed real `#print axioms` output this session): all four new
  headline theorems depend only on `[propext, Classical.choice, Quot.sound]` —
  base three, no `native_decide`, no new axiom.
- Committed as **9c1d838**. Not pushed (no egress; host pushes). HANDOFF.md and
  FRONT-A-ROUTES.md updated in the same commit.
- Scope target `sorry-free:CollatzMoonshot/FrontA/BackwardTwoThirdsStopping.lean`
  met; `box done --green` signalled (treadmill will not relaunch).

## 🎬 Next actions

The exponent-`2/3` frontier is now unconditional, exactly matching the `1/2`
frontier. The open mathematics moves to the `FRONT-A-ROUTES.md` ladder:

1. **Step 5 (the real crux now)**: decide experimentally, then prove, whether the
   correct statement is *pointwise* harmonic growth or a *uniform* bound. The
   3-adic adversary (`experiments/barrier_adversary.py`) is the falsification side.
   This is where the genuine open difficulty lives — the frontier cardinality
   results are only useful if enough uniformity survives the adversary.
2. Step 6: the overlap/packing question — do the first-exit basins of distinct
   roots overlap enough to spoil aggregate mass? (`experiments/barrier_tree.py`,
   `barrier_tail.py`.)
3. Only if uniformity survives: return to `DivergentTailHarmonicBudget` and annular
   packing (would merge Route A2 back into the A1/A3 rigidity problem).

## ⚠️ Gotchas

- `push_neg` is deprecated in this toolchain → use `push Not` (matches
  `BackwardStopping.lean` idiom; I already did).
- `#print axioms` must be run via a throwaway file compiled with `lake env lean`
  (create `CollatzMoonshot/AxCheckTmp.lean`, run, delete) — there's no repl.
- Don't create a `HANDOFF.md` symlink; `HANDOFF.md` here is the master running log
  (I prepended a new dated section to it this lap).

## 📁 Key files

- `CollatzMoonshot/FrontA/BackwardTwoThirdsStopping.lean` — this lap's deliverable.
- `CollatzMoonshot/FrontA/BackwardStopping.lean` — the `1/2` template + all reused
  value-based lemmas.
- `CollatzMoonshot/FrontA/BackwardTwoThirdsRenewal.lean` — pinned `def`s
  (`TwoThirdsRepeatOrStoppingGrowth`, `TwoThirdsStoppingFrontier`,
  `TwoThirdsStoppingFrontier.card_bound`), edge weight + telescoping.
- `CollatzMoonshot/FrontA/BackwardTwoThirds.lean` — the 270-state certificate,
  `InFiveHeightState`, `twoThirdsPotential`, `exists_twoThirdsExpansion`.
- `FRONT-A-ROUTES.md` — the chippable ladder; steps 5-6 are the open frontier.
- `experiments/barrier_adversary.py` — the 3-adic falsification harness for step 5.

---
**→ Next session: this is your starting point. Don't summarize this doc back to
Trevor or wait for instructions, and don't offer other projects from the KB — this
doc IS the chosen thread. The scoped stopping-line target is complete and committed;
pick up at "Next actions" #1: the pointwise-vs-uniform harmonic-growth question
(ladder step 5), starting from `experiments/barrier_adversary.py` and the numerical
critical exponents already recorded in FRONT-A-ROUTES.md / HANDOFF.md.**
