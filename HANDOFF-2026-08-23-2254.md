# Handoff: exponent-2/3 real telescoping renewal layer Lean-checked

**Date**: 2026-08-23 22:54 UTC · **Branch**: main · **HEAD**: `f95cdf1`

## 🎯 What we're doing
Front A of the Collatz moonshot: growing the barriered backward tree fast enough.
This lap's scoped objective was `sorry-free:CollatzMoonshot/FrontA/BackwardTwoThirdsRenewal.lean`
— the exact real telescoping layer that upgrades the rational 270-state exponent-`2/3`
certificate (`BackwardTwoThirds.lean`) to the exact real edge weight `(x/y)^(2/3)`. This is
the analytic half of the `1/2`→`2/3` upgrade, the analogue of `BackwardRenewal.lean` for the
half-exponent certificate.

## ✅ State (all committed at `f95cdf1`, not pushed)
- `CollatzMoonshot/FrontA/BackwardTwoThirdsRenewal.lean` added, **sorry-free**, imported from
  `CollatzMoonshot.lean`. Full `lake build` green (8,738 jobs), re-verified by the pre-commit
  hook. Axiom audit (real `#print axioms` output): every headline theorem depends only on
  `[propext, Classical.choice, Quot.sound]`; **no `native_decide`**.
- Contents (the six operator-requested pieces):
  1. `twoThirdsEdgeWeight x y := ((x:ℝ)/(y:ℝ)) ^ (2/3:ℝ)` (`Real.rpow`),
     `twoThirdsRationalWeight j := (52/25)·(629/1000)^j`;
     `twoThirdsRationalWeight_lt_edgeWeight` — strict underweight on every actual positive odd
     block `3y+1 = 2^j x`, via `twoThirdsRationalWeight_cube_lt` (`(52/25)^3 < 9`,
     `4·(629/1000)^3 < 1`), `3/2^j < x/y`, and `Real.rpow` monotonicity. Cube-root step:
     `lt_of_pow_lt_pow_left₀` after `((3/2^j)^(2/3))^3 = (3/2^j)^2` from `Real.rpow_mul`/
     `Real.rpow_natCast`.
  2. `twoThirdsEdgeWeight_mul` — exact telescoping through positive endpoints
     (`Real.mul_rpow` + `field_simp`).
  3. `exists_twoThirdsChildFinset` — upgrades `exists_twoThirdsExpansion` to real-weight
     expansion over a nonempty, value-injective finite child set of `Fin 5 × ℕ`
     (`TwoThirdsChildAt`, `TwoThirdsValuesInjective`): actual reusable edges, floor-safe
     five-height states, `3 c < 2^23 x`, strict expansion under `(x/c)^(2/3)`.
  4. `twoThirdsPotential_pos` (unit residues, every floor) and `twoThirdsPotential_le_max`
     (uniform ceiling `1051827`, `decide +kernel` over `Fin 5 × Fin 81`).
  5. `TwoThirdsStoppingFrontier` / `TwoThirdsRepeatOrStoppingGrowth` — defined exactly
     analogous to `NetHalfStoppingFrontier` / `NetHalfRepeatOrStoppingGrowth`, root state is
     `twoThirdsPotential (0 : Fin 5) d`.
  6. `TwoThirdsStoppingFrontier.card_bound` — `V(0,d) < card(S)·(1051827·(d/H)^(2/3))`
     (`Real.rpow_le_rpow` + `Finset.sum_le_card_nsmul`).

## 🎬 Next actions (next lap's crux)
1. **Prove the recursive `TwoThirdsRepeatOrStoppingGrowth`** — the last open obligation of the
   `2/3` route, the exact analogue of the already-discharged `NetHalfRepeatOrStoppingGrowth`
   (`BackwardStopping.lean`). Port the fuel-`H+1` frontier recursion from
   `BackwardStopping.lean`, replacing:
   - `InHeightState`/`Bool` height nodes  →  `InFiveHeightState`/`Fin 5` nodes;
   - `exists_netHalfChildFinset`  →  `exists_twoThirdsChildFinset` (already built this lap);
   - `netHalfEdgeWeight`/`sqrt`  →  `twoThirdsEdgeWeight`/`(·)^(2/3)`;
   carry `d ≥ 2` throughout (the five-state transitions need it — unlike the two-state ones).
   The collision→cycle wiring uses `ReusableOddBlockChild` and `reusableOddBlockChild_parent_eq`
   (both already in `BackwardRenewal.lean`, imported); mass growth reuses the generic
   `weighted_biUnion_expands` with `twoThirdsEdgeWeight` telescoping (`twoThirdsEdgeWeight_mul`).
2. Then compose with `TwoThirdsStoppingFrontier.card_bound` to get the exponent-`2/3` upgrade
   of the headline: every odd unit root `d ≤ H` has either a cycle above `d` or
   `> V(0,d)/(1051827)·(H/d)^(2/3)` distinct first exits.

## ⚠️ Gotchas (this lap)
- `pow_le_pow_left` is renamed to **`pow_le_pow_left₀`**, and the strict cube-root direction is
  **`lt_of_pow_lt_pow_left₀`** (both burned no cycles this lap only because the corpus note
  `lean-pow-le-pow-left-renamed.md` warned first).
- `BackwardRenewal.lean` and `BackwardTwoThirds.lean` BOTH declare
  `sevenCostsAt_le_twentyThree`. Importing both (as this file and `CollatzMoonshot.lean` do)
  compiles fine, but **do not reference that name** from a file importing both — it is
  ambiguous. This lap sidesteps it entirely (the `3 c < 2^23 x` bound comes pre-packaged out
  of `exists_twoThirdsExpansion`). See corpus `lean-namespace-collision-multi-import.md`.
- Casting the rational certificate to ℝ: `by_cases` on `fiveHasShrink i x` FIRST, reduce the
  `if` with `simp only [hs, if_true]` / `[hsf, Bool.false_eq_true, if_false, add_zero]`, THEN
  `(Rat.cast_lt (K := ℝ)).mpr` + `push_cast` + `convert … using 2` — avoids casting an `ite`.
- `2/3` critical-exponent reality check unchanged: `2/3 < 1`; the numerical critical exponent
  on this state space is only `≈ 0.6838`. Do NOT claim harmonic (exponent-1) growth.

## 📁 Key files
- `CollatzMoonshot/FrontA/BackwardTwoThirdsRenewal.lean` — this lap's module.
- `CollatzMoonshot/FrontA/BackwardTwoThirds.lean` — the rational 270-state certificate + local
  expansion this lap upgrades.
- `CollatzMoonshot/FrontA/BackwardStopping.lean` — the half-exponent recursion to port (next
  lap, step 1 above).
- `CollatzMoonshot/FrontA/BackwardRenewal.lean` — half-exponent analogue of this lap's module.
- `FRONT-A-ROUTES.md` — route map; step 5 updated, remaining work is the recursive theorem.

---
**→ Next session: pick up at "Next actions" #1 — the recursive `TwoThirdsRepeatOrStoppingGrowth`.
All its local inputs (`exists_twoThirdsChildFinset`, telescoping, cardinal bound, cycle wiring)
are now in place; the work is the frontier-recursion bookkeeping ported from `BackwardStopping`.**
