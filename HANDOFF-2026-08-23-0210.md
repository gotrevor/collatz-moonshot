# Handoff: Front B dictionary landed — sorry-free, axiom-clean

**Date**: 2026-08-23 02:10 UTC · **Branch**: main · **HEAD**: `a715e89`

## 🎯 What we're doing

Collatz moonshot, Front B (no nontrivial cycles). This lap's scoped objective —
`sorry-free:CollatzMoonshot/FrontB/Dictionary.lean` — is **complete**. The
Halbeisen-Hungerbühler / Bernstein-Lagarias dictionary
`noNontrivialCycle_iff_frontB : NoNontrivialCycle ↔ FrontB` is proved, and
`#print axioms` on it shows exactly `[propext, Classical.choice, Quot.sound]`
(observed in real build output this session). Full `lake build` green; pre-commit
hook re-verified at commit time.

## 🧠 Context to carry forward

- **The handed-off target was refutably false.** `IsTrivial v := numer v = den v`
  (member = 1) excluded `[false, true]` — the trivial accelerated cycle rooted at 2,
  an `IntegerCycle` with `numer 2, den 1`. Fixed in `Threads.lean`:
  `IsTrivial v := numer v = den v ∨ numer v = 2 * den v` (member ∈ {1,2}, the exact
  membership of the trivial `tstep`-cycle). All other `IsTrivial` uses are opaque
  hypotheses, so nothing else moved. Lesson: probe frozen targets on tiny witnesses
  before proving.
- **Layer 1 engine** (replaced the old HANDOFF's suggested rotation-invariant
  induction, which is fiddlier): the *unconditional* iterate identity
  `tstep_iterate_identity : 2^m · tstep^[m] n = 3^(ones v) · n + numer v` for
  `v = traceWord n m`, one plain induction over ℕ. On a cycle it gives
  `member_identity` via `linear_combination`. General trick: when a cyclic identity
  resists, prove the acyclic generalization it specializes from.
- **Layer 2** is the `walk` lemma: chase a `step`-cycle with `tstep` under a
  hit-or-overshoot invariant carrying `d ≤ 2j` (forces positive accelerated period).
  Only `3w+1`-intermediates of odd members are missable; an intermediate of an
  intermediate is parity-impossible.
- **Converse** uses the rotation identities per step (`realize_head_false/true`),
  `rot = List.rotate 1` + `List.rotate_length` to close the loop, expansion
  `tstep^[p] = step^[K]`, and excludes member 4 (`four_not_tstep_periodic`).
- Consequence: every `FrontB/Threads.lean` theorem is now a genuine ℕ-Collatz
  statement (its "owed lemma" warning was replaced with the ✅ note).
- **Not mine, left uncommitted deliberately**: dirty `Rigidity*`, `FrontA/`,
  `README.md`, notes/papers files from a previous session. Do not sweep them into a
  commit blindly.

## ✅ State

- `FrontB/Dictionary.lean`: 0 sorries, both frozen headlines proved.
- Axiom audit observed: all three headline theorems depend only on
  `[propext, Classical.choice, Quot.sound]`.
- Committed green: `a715e89` (Dictionary.lean, Threads.lean, HANDOFF.md).
- `box done --green` signalled; treadmill stopping after this lap.
- Master `HANDOFF.md` updated to the DONE state (repo's existing single-file scheme).

## 🎬 Next actions

1. The scoped run is finished — nothing owed on Dictionary. If a new lap opens on
   Front B, the hardest open obligation is **`Compression`** (`Threads.lean`): a
   nontrivial integral cycle has boundedly many circuits. No formal work exists
   toward it; prose in `FRONT-B-ROUTES.md` thread 1. Note
   `experiments/circuits3.py`'s warning that numerics can't guide it.
2. Alternative thread: `BoundedDen` (~12%), or Front A rigidity (`FrontA/`,
   `Rigidity/` — check the uncommitted prior-session diffs first).

## ⚠️ Gotchas

- `rw [← h]` where the local was introduced by `set` rewrites inside the local's own
  definitional subterms → self-referential goals. Prove forward, `rw ... at`.
- After `rw [← Function.iterate_succ_apply']` goals display `f^[(m-1).succ]`, so
  `rw [show m-1+1 = m ...]` won't fire; restructure via a `have` on `f^[m-1+1]`.
- This toolchain (Lean v4.31 pin): `Nat.pos_pow_of_pos` gone (use `Nat.pow_pos`),
  `le_or_lt` absent (use `lt_trichotomy`).

## 📁 Key files

- `CollatzMoonshot/FrontB/Dictionary.lean` — the finished bridge; module docstring
  documents the structure and the IsTrivial correction.
- `CollatzMoonshot/FrontB/Threads.lean` — corrected `IsTrivial`; the open-thread board.
- `HANDOFF.md` — master handoff (DONE state, next-attack pointer).

---
**→ Next session: this is your starting point. Don't summarize this doc back to
Trevor or wait for instructions, and don't offer other projects from the KB — this
doc IS the chosen thread. The scoped objective is DONE; if relaunched on this repo,
absorb the context above and start at "Next actions" #1: attack `Compression`.**
