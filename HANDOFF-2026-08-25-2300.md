# HANDOFF 2026-08-25-2300  (★ leg-2 GO grind: single-kernel effective-measure machinery COMPLETE ★)

- **Branch:** `main`  **HEAD:** `a060fb1`  **Build:** 🟢 `lake build` 8754 jobs.  **Tree:** clean.
- **Read first:** `DIRECTION.md` CURRENT DIRECTIVE (GO route — do NOT edit), `PENDING_WORK.md` top,
  `STATUS.md`, then this. New module this run: `CollatzMoonshot/FrontA/Legendre.lean`.

## What this run did — built the entire single-kernel Padé/Legendre effective-measure toolkit

The crux `sep_two_three` (`PowSeparation.lean:38`, sole `src/` sorry) is reduced sorry-free to ONE
Gelfond/Rhin effective measure of `log₂3` (the `hmeas` / `sep_of_uniform_measure` interface, unchanged).
This run **attacked the measure construction itself** (leg 2 of the 3-leg plan in `Gelfond.lean`),
building — from scratch or faithfully adapted from [`ahhwuhu/zeta_3_irrational`](https://github.com/ahhwuhu/zeta_3_irrational) and
toolchain-adapted (v4.18→v4.33) — the full machinery for a linear form `Λ_n(a) = ∫₀¹ P_n(y)/(1-a·y)`.
ALL trust-base clean (`[propext, Classical.choice, Quot.sound]`), each its own green commit:

**Source read (route-decisive):** proven object is **Rhin 1987**, lin-indep measure of `{1,log2,log3}`
`< 7.616` (refined Wu, *Math.Comp.* 72 (2003)); Salikhov μ(log3)≤5.125. ⟹ honest exponent in `hmeas`
is `C≈7`, NOT `C=6` (flagship `sep_two_three_of_gelfond_measure` is illustrative; honest interface is
`sep_of_uniform_measure` at the true C). Voutier 2111.01044 is roots-of-rationals, OFF-path for logs.

**`PowSeparation.lean` addition:** `pow_le_two_pow_gen` (`k^C≤2^k` for `k≥C²`, C≥4) + `nat_sq_le_two_pow`
+ `exp_half_lt_two` — general explicit crossover; removes the last non-explicit dep from the reduction
at ANY exponent.

**`Legendre.lean` (new, the leg-2 toolkit):**
- `shiftedLegendre` + `shiftedLegendre_eq_sum` + `shiftedLegendre_eq_int_poly` (integer coeffs
  `(-1)^k C(n,k) C(n+k,n)`) — approximant backbone.
- `shiftedLegendre_poly_eval_{zero,one}_eq_zero` — order-n vanishing (Padé contact).
- `integral_shiftedLegendre_mul_eq` — **n-fold IBP identity** `∫₀¹ P_n·f = ((-1)^n/n!)∫₀¹ (y(1-y))^n f⁽ⁿ⁾`
  (proved from scratch; general smooth f).
- `mobius_iterate_deriv` — `dⁿ/dxⁿ[1/(1-a·x)] = n!aⁿ/(1-a·x)^(n+1)` (neighborhood-congruence induction).
- `legendre_mobius_integral` — Padé remainder form `∫₀¹ P_n/(1-a·y) = (-a)^n ∫₀¹ (y(1-y))^n/(1-a·y)^(n+1)`.
- `legendre_mobius_remainder_bound` — `∫₀¹ (y(1-y))^n/(1-a·y)^(n+1) ≤ (1/4)^n/(1-a)^(n+1)` ⟹ geometric
  decay `|Λ_n| ≤ (|a|/4(1-a))^n/(1-a)` (ρ<1 for a<4/5).
- `mobius_base_integral` `∫₀¹1/(1-a·y)=-log(1-a)/a`; `mobius_moment_rec` `a∫y^{k+1}=∫y^k-1/(k+1)`;
  `mobius_moment_closed` `∫₀¹y^k/(1-a·y)=r_k - log(1-a)/a^{k+1}`.
- `legendre_mobius_linear_form` — **`∫₀¹ P_n/(1-a·y) = A + B·log(1-a)`** (A=∑c_k r_k, B=-∑c_k/a^{k+1}).
- `legendre_mobius_ne_zero` — **`Λ_n ≠ 0`** (leg-3's provable half; remainder integrand >0 on (0,1)).

⟹ For a SINGLE Möbius kernel the three effective-measure facts — **linear form + geometric remainder
bound + non-vanishing** — are ALL machine-checked. That is a complete effective irrationality measure
of `log(1-a)` modulo rationality/denominator bookkeeping.

## ★ NEXT LAP — finish leg 2, then leg 3 ★
1. **Rationality/denominator tracking (mechanical, source-independent).** Re-cast `r_k` (and hence A,B)
   as ℚ; show `lcm(1..n)·(a^{n}·A_n)`, `lcm(1..n)·(a^{n}·B_n) ∈ ℤ` when `a` is an integer-reciprocal
   (so `1-a`∈{1/2,1/3,...} type; `log(1-a)=∓log2/log3`). Uses `Gelfond.lcmUpto_le` (leg 1). Yields
   `D_n·Λ_n = p_n + q_n·log(1-a)`, `p_n,q_n∈ℤ`, nonzero (from `legendre_mobius_ne_zero`), `|·|≤D_n ρ^n`.
   This is a genuine effective measure of a SINGLE log — big milestone, still not the crux.
2. **Leg 3 (the ONLY genuinely source-gated piece).** The crux needs a linear form in `log2` AND `log3`
   *simultaneously* (Rhin's two-kernel / determinant construction), not one log. Need the exact Rhin
   kernel (which two `a`'s, the determinant non-vanishing). Precise fetch filed in `ON-LINE-REQUEST.md`
   (2026-08-25). If `ON-LINE-FINDINGS-*` has landed, formalize the two-kernel determinant; else keep
   pushing item 1 (fully independent).
3. **BASELINE fallback (does NOT clear gate):** cite `gelfond_two_three_measure` in `Assumed/`.

## Other fronts — UNCHANGED
Front A M2′ COMPLETE; parity-reconstruction + harmonic-dual COMPLETE/BASELINE; Front B on hold.
Paradoxical project classification: **PROMISING EVIDENCE** (crux open; now with substantial in-repo
progress on the measure construction — leg 1 + full single-kernel leg 2 machinery).

## Axiom audit
`sep_two_three` unchanged (disclosed sorry). ALL new lemmas this run (`PowSeparation` crossover +
entire `Legendre.lean`) are trust-base only `[propext, Classical.choice, Quot.sound]`. No new axiom,
no new sorry. `le_two_blocks_not_acyclicParadoxical` footprint unchanged.
