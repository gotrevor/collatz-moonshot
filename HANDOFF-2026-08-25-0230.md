# HANDOFF 2026-08-25-0230  (★ FRONT-A-PARADOXICAL delivered A/B/C/D → PROMISING EVIDENCE ★)

- **Branch:** `main`  **HEAD:** `0ec3c0c`  **Tree:** clean  **Build:** 🟢 `lake build` 8751 jobs.
- **Read first, in order:** `DIRECTION.md` (CURRENT DIRECTIVE — outranks this file; do NOT edit
  it), `PENDING_WORK.md` (top block = this project's live state + next attacks),
  `FRONT-A-PARADOXICAL.md` (RESULTS / Part-D classification at bottom), then this file.

## What this run did: executed `FRONT-A-PARADOXICAL.md` end to end

Per DIRECTION's MANDATED move (execute the paradoxical-segments project, not just its
census). All four parts delivered; classified **PROMISING EVIDENCE**.

### New files
- `experiments/paradoxical.py` — exact-integer machine. Part A source lock (identity,
  criterion (P), slack (S), `7→8`), paradoxical-start census, Niu CF-pattern test, the
  `≥3-odd-blocks` word/residue enumerator, AND an independent orbit-based second implementation.
- `CollatzMoonshot/FrontA/Paradoxical.lean` — defs + criterion + few-block `numer` closed
  forms + RT Appendix A + reconstruction lemmas. Wired into `CollatzMoonshot.lean`.
- `CollatzMoonshot/Assumed/Paradoxical.lean` — RT Thm 3.2 axiom + the full Front-A consumption.

### Proved in Lean (all trust-base clean unless noted)
- `slack_identity` (S), `paradoxical_criterion` (P), `acyclicParadoxical_criterion` — endpoint
  `n≤y` ⇔ pure word inequality `d·n ≤ numer`.
- `numer_singleBlock = 2^s(3^q−2^q)`, `numer_twoBlock = 3^d(3^b−2^b)+2^(b+c)(3^d−2^d)` (via a
  ℤ true-prefix recursion `numer_true_prefix`).
- **`headBlock_not_acyclicParadoxical` = Rozier–Terracol Appendix A, formalized.** A pure head
  odd-block `[T]^q[F]^t` is never acyclic paradoxical (from `2^m y + 2^q = 3^q(n+1)` + subcrit).
- **Part C consumption — machine-checked, no `sorryAx`:**
  `finite_acyclicParadoxical_imp_noDivergent : FiniteAcyclicParadoxical → NoDivergentOrbit`,
  ledger `[propext, Classical.choice, Quot.sound, rozier_terracol_3_2]`. Whole divergence
  bridge discharged: `exists_standardStop_of_diverges` (running-min), `tstep_iterate_eq_step_
  iterate` + `infiniteStoppingTime_of_diverges` (shortcut embedding), `step_le_shortcut` +
  `not_tstep_fixed_of_diverges` (acyclicity), `diverges_two_pow_mul`.
- Reconstruction infra for the open crux: `headBlock_dvd_succ` (`[T]^b` head ⇒ `2^b ∣ n+1`;
  `[propext, Quot.sound]`) and `traceWord_add` (word decomposition at any step).

### Discovery (experiment, two independent implementations)
**Every acyclic paradoxical word has ≥ 3 odd blocks.** Word/residue enumerator: all ≤2-block
front-normalized words to length 30, two-block to length 38, zero paradoxical. Independent
orbit-based verifier: min odd-block count = 3 over ALL windows for starts ≤ 100000. Strictly
generalizes RT Appendix A (single block only). Niu's CF pattern: not falsified (all observed
ratios in the `log₃2` convergent/semiconvergent family; `n ≤ 200000`).

## The one open obligation on this front (the live crux)
`le_two_blocks_not_acyclicParadoxical` — the interior two-block exclusion, the sole disclosed
`src/` sorry (`FrontA/Paradoxical.lean`, ~line 309). This is the active-crux decomposition, NOT
off-path — do not relocate it to `wip/`.

**Why it's hard (established this run):** reduces via (P) + `numer_twoBlock` to a lower bound on
the realizing start `n`. The 2-adic half is proved (`headBlock_dvd_succ`); the real obstruction
is the **joint 2-adic/3-adic residue constraint** `3^b ∣ 2^c X + 1` on the interior odd value
`X = x_{b+c} ≥ 2^d − 1`. Simple 2-adic propagated bounds are provably insufficient (tested:
`n ≥ 2^b−1` → 1457 violations; the interior bound `3^b(n+1) ≥ 2^(b+c)(2^d−1)+2^b` → 318
violations). Genuinely deep — narrow lap by lap, do not file infeasible.

**Precise held-out conjecture** (in `FRONT-A-PARADOXICAL.md` RESULTS): for every
front-normalized `[T]^b[F]^c[T]^d[F]^e` with `3^{b+d} < 2^{b+c+d+e}`, the least realizing `n`
satisfies `(2^{b+c+d+e}−3^{b+d})·n ≥ 3^d(3^b−2^b)+2^{b+c}(3^d−2^d)`.

**Next concrete attack:** use `traceWord_add` to split the word after step `b` and after step
`b+c+d`, extracting (i) `2^b ∣ n+1` (have it), (ii) the interior even-run giving
`x_{b+c} = x_b/2^c` odd, (iii) the second block `2^d ∣ x_{b+c}+1`. Then formalize the minimal
interior `X` selected by `3^b ∣ 2^c X+1` and close the inequality. This is a multi-lemma effort.

## Other fronts — UNCHANGED (do not touch without a new directive)
- Front A M2′ COMPLETE; parity-reconstruction COMPLETE/BASELINE; harmonic-dual COMPLETE.
- Front B `Compression` on hold (SdW 2005 source-blocked). `OneCircuit` a≥2 = Steiner/Baker,
  off path (`SteinerOneCircuit` hypothesis). Do NOT rebuild any of these.

## Axiom footprint added this run
`Assumed.rozier_terracol_3_2` (RT Thm 3.2, THEOREM-grade, faithful constructive form). All new
`FrontA/Paradoxical.lean` theorems trust-base clean; consumption theorem =
trust base + that one axiom, no `sorryAx`.
