# HANDOFF 2026-08-25-1600  (★ two-block crux DECOMPOSED: reduction PROVED, one clean sorry left ★)

- **Branch:** `main`  **Build:** 🟢 `lake build` 8752 jobs.  **Tree:** clean at commit time.
- **Read first:** `DIRECTION.md` CURRENT DIRECTIVE (GO route; do NOT edit), `PENDING_WORK.md` top
  (DONE 1600 + GO ROUTE FULLY SCOPED), then this.

## What landed this lap
Replaced the bare `have hbd : b + d ≤ 5 := sorry` (the sole `src/` crux sorry) with a
machine-checked reduction to ONE clean, isolated, weak-Baker lemma.

**New module `CollatzMoonshot/FrontA/PowSeparation.lean`** (sorry-free except `sep_two_three`):
- `grow_two_three` — `3^(k+2) ≤ 2^(2k−3)` for `k ≥ 15` (elementary `Nat.le_induction`).
- `finite_two_block_check` — `native_decide`: no near-critical config for `6 ≤ k ≤ 14`.
- `bd_reduction` — THE reduction: window `3^k<2^m<2·3^k` + `(W) 2^d(2^m−3^k)<2^m` +
  `(A) (2^m−3^k)·2^k ≤ 2^m·3^d` + `sep_two_three` ⇒ `k ≤ 5` (β=1/3 gives `k≤14`, finite check
  clears `[6,14]`). PROVED.
- `sep_two_three` — **the sole disclosed sorry**: `3^(3k) ≤ (2^m−3^k)^3·2^k` for near-critical
  `k ≥ 6` (⇔ `|2^m−3^k| ≥ 3^k·2^(−k/3)`). Clean, weak, TRUE (checked exactly to `k<500`).

**In `near_critical_containment`** (`Paradoxical.lean`): `hbd` now derives `(W)` from the proved
`hbracket`, and `(A)` from ¬A (`hhi`/`w≥1`/`hU1lt` give `D ≤ 3^d·2^c`, plus `2^c ≤ 2^(c+e)`), then
calls `bd_reduction`. No other `src/` change.

**Axiom audit:** `le_two_blocks_not_acyclicParadoxical` = `[propext, sorryAx (=sep_two_three),
Classical.choice, Quot.sound, finite_two_block_check native_decide ax]`. The `sorryAx` is now the
clean isolated separation (not the opaque `b+d≤5`); the `native_decide` artifact is 🟢 finite.
`finite_acyclicParadoxical_imp_noDivergent` unchanged (`+ rozier_terracol_3_2`, no sorry).

## ★ NEXT LAP — discharge `sep_two_three` (the GO prize) ★
Prove `sep_two_three : 6 ≤ k → 3^k < 2^m → 2^m < 2·3^k → 3^(3k) ≤ (2^m−3^k)^3·2^k`.
This is a **weak** effective irrationality bound for `log₂3` (β=1/3 is far inside the feasible
window `[0.319, 0.387)`), NOT full Baker. Route:
1. mathlib HAS `Mathlib/Algebra/ContinuedFractions/` (`Determinant.lean`: `|pₙqₙ₊₁−pₙ₊₁qₙ|=1`,
   `ContinuantsRecurrence`, `ConvergentsEquiv`) — the best-approximation lower-bound machinery.
   No ready-made effective measure for `log₂3`.
2. Realize `|k·log3 − m·log2|` (equiv. `2^m/3^k` near 1) via the CF of `log₂3 =
   [1;1,1,2,2,3,1,5,2,23,…]`; a computed initial CF segment + the convergent gap gives the weak
   `2^(−k/3)` bound. Because β=1/3 is loose, a crude bound from a finite CF prefix should suffice.
3. Alternative granularity if the full CF proof stalls: prove a `native_decide`/finite base for
   small `k` + an inductive/analytic tail. Or (BASELINE, NOT for gate-clearing) cite it as a narrow
   axiom.
If out of reach this lap, `sep_two_three`'s disclosed sorry STAYS; narrowing it (e.g. proving the
`k`-range base or the analytic step) is a valid advance.

## Other fronts — UNCHANGED
Front A M2′ COMPLETE; parity-reconstruction COMPLETE/BASELINE; harmonic-dual COMPLETE. Front B on hold.
